# Правило авторизации в HRM HuntTech через CDP / Browser

При входе в веб-интерфейс HRM HuntTech (`http://localhost:8080/hrm/`) через Chrome DevTools Protocol (CDP) / браузер:

1. **Обязательная очистка полей**: Перед вводом логина и пароля на экране логина всегда предварительно полностью стирать/очищать старые данные в полях ввода (select all + backspace / clear), чтобы исключить склеивание символов.
2. **Учётные записи для работы и тестирования**:
   - **Администратор**: логин `admin`, пароль `admin`
   - **Пользователь группы Management**: логин `alan`, пароль `Dodo-2012`
   - **Пользователь рекрутинга**: логин `aten`, пароль `1234`
