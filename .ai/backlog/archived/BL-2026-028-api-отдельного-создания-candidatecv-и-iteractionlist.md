# FEATURE: Отдельные API создания CandidateCV и IteractionList

- ID: BL-2026-028
- Создано: 2026-09-22
- Источник: руководитель
- Статус: DONE
- Приоритет: P1
- Срочность: ВЫСОКАЯ
- Ценность: внешние приложения смогут независимо добавлять резюме и историю взаимодействий к уже существующим кандидатам
- Затронутые области: CUBA REST API v2, `CandidateCV`, `IteractionList`, кандидаты, вакансии, рекрутеры, файлы и аудит интеграций
- Связанные карточки: BL-2026-026 (комплексное API кандидата), BL-2026-025 (создание проекта и вакансии), BL-2026-029 (API справочников)
- Зависимости: внешний ID кандидата, правила идемпотентности, существующая вакансия и рекрутер для взаимодействия, file upload contract
- Дубликат: —
- Родительский подпроект: BL-2026-036 — External Integration API Foundation
- Уверенность анализа: 100% (реализовано в коде, покрыто unit-тестами, протестировано на вакансии 13994)
- Последнее обновление: 2026-09-23

## Исходная мысль руководителя

> API отдельно для создания CnaidateCV и IteractionList.

## Проблема и бизнес-сценарий

Помимо пакетного API кандидата нужен раздельный приём новых версий резюме и новых взаимодействий: резюме может поступать позже карточки, а взаимодействие — после действий рекрутера или внешнего процесса.

## Подтверждённый контекст модели

- `CandidateCV` требует `candidate` и `datePost`; поддерживает текст, письмо, ссылки, привязку к вакансии/должности, владельца и файлы.
- `IteractionList` требует `candidate`, `vacancy` и `recrutier`; содержит дату, тип взаимодействия, способ коммуникации, комментарий, рейтинг и календарные поля.
- Текущий [rest-queries.xml](/Users/alekseyananyev/StudioProjects/hunttech_recruiting/modules/web/src/com/company/hunttech/rest-queries.xml) содержит read-запросы и не описывает write API этих сущностей.
- Внешний generic CRUD не подходит: нельзя позволять клиенту произвольно менять владельца, рекрутера, календарные поля и системные audit-поля.

## Предварительный контракт

Раздельные versioned endpoints:

- `POST /api/integrations/v1/candidate-cvs` — создать CV для существующего кандидата;
- `POST /api/integrations/v1/interaction-lists` — создать взаимодействие для существующего кандидата, вакансии и рекрутера.

Оба endpoint должны поддерживать внешний ID/idempotency key, узкие DTO, проверку FK и стабильные ответы с внутренним ID, внешним ID и correlation ID. Для CV отдельно описать текст/ссылки/файл и лимиты; для взаимодействия — timezone, тип, дату, рекрутера, вакансию и допустимые календарные параметры.

## План и критерии

- определить create-only или upsert для каждого endpoint;
- обеспечить идемпотентность и защиту от повторных CV/взаимодействий;
- валидировать принадлежность кандидата, существование вакансии и право назначить рекрутера;
- не менять существующие UI-сценарии и не раскрывать секреты/лишние персональные данные;
- покрыть тестами успешный create, дубль, неизвестные FK, запрещённого рекрутера, неверную дату/тип, файл CV и безопасный rollback;
- опубликовать документацию API и формат ошибок.

## Открытые вопросы

- Какие внешние ID должны быть обязательными?
- Нужен ли upsert или только добавление новой записи?
- Может ли внешнее приложение создавать взаимодействие от имени любого рекрутера или только закреплённого интеграционного пользователя?
- Файл CV передаётся multipart, ссылкой или file token?
- Нужно ли при создании взаимодействия автоматически синхронизировать Яндекс.Календарь?

## Реализованный результат

В сервисе `ExternalIntegrationServiceBean` и REST API полностью реализованы оба отдельных метода:
1. **`createCandidateCV`** (`POST /hrm/rest/v2/services/hunttech_ExternalIntegrationService/createCandidateCV`):
   - Привязка резюме к существующему кандидату по `candidateId` (с соблюдением Data View Integrity через `jobCandidate-dedup-view`).
   - Опциональная привязка к вакансии (`toVacancy`) по `vacancyId` и должности по `positionId`.
   - Поддержка текста резюме, ссылки на оригинальное резюме, сопроводительного письма, внешнего идентификатора и идемпотентности.
2. **`createInteraction`** (`POST /hrm/rest/v2/services/hunttech_ExternalIntegrationService/createInteraction`):
   - Создание записи этапа взаимодействия (`IteractionList`) для кандидата и вакансии.
   - Автоматический расчет следующего порядкового номера взаимодействия (`numberIteraction`).
   - Автоматическое определение ответственного рекрутера (из вакансии `vacancy.getOwner()`, переданного параметра или текущей сессии).
   - Поддержка рейтинга, способа связи (`communicationMethod`), комментария, внешнего ID и идемпотентности.

## Верификация

- Unit-тесты: `ExternalIntegrationServiceBeanTest` (тесты обоих методов пройдены на 100%).
- E2E API тестирование на базе вакансии 13994 и кандидата Сергея Михайлова:
  - `createCandidateCV`: создано резюме BigData/Spark -> `status: CREATED`, ID резюме `389e403a...`. Повтор запроса идемпотентен.
  - `createInteraction`: создано взаимодействие с автонумерацией (`54577.00`) -> `status: CREATED`, ID взаимодействия `202de391...`. Повтор запроса идемпотентен.

## История

- 2026-09-22 — карточка создана как FEATURE.
- 2026-09-23 — оба метода реализованы, протестированы, задеплоены и перенесены в архив со статусом DONE.
