# FEATURE: API создания кандидата, резюме и взаимодействий

- ID: BL-2026-026
- Создано: 2026-09-22
- Источник: руководитель
- Статус: NEW
- Приоритет: не определён
- Срочность: не определена
- Ценность: внешние приложения смогут передавать в HRM полный профиль кандидата и историю работы с ним без ручного повторного ввода
- Затронутые области: CUBA REST API v2, интеграционный сервис, `JobCandidate`, `CandidateCV`, `IteractionList`, файлы резюме, вакансии, пользователи-рекрутеры, дедупликация и аудит
- Связанные карточки: BL-2026-025 (API создания проекта и вакансии), BL-2026-001 (будущее BLOB-хранение файлов), BL-2026-004 (AI-извлечение контактов из резюме), BL-2026-024 (позиционирование кандидата после умной загрузки)
- Зависимости: внешний идентификатор кандидата/резюме, правила дедупликации, авторизация внешнего клиента и источник вакансии для взаимодействия
- Дубликат: —
- Уверенность анализа: высокая для модели связей и обязательных FK; низкая для окончательного payload и политики дублей до согласования внешнего клиента
- Последнее обновление: 2026-09-22

## Исходная мысль руководителя

> так же надо сделать API создания кандидата: карточки, резюме, взаимодействий

## Проблема и бизнес-сценарий

Внешнее приложение должно передавать в HRM комплект данных кандидата: основную карточку, одно или несколько резюме и взаимодействия по вакансиям. Система должна сохранить связи и вернуть внешний результат с ID созданных объектов, не создавая дубли при повторной доставке.

## Что проверено в проекте

### Текущий REST-контур

- [rest-queries.xml](/Users/alekseyananyev/StudioProjects/hunttech_recruiting/modules/web/src/com/company/hunttech/rest-queries.xml) сейчас содержит read-запросы для вакансий и справочников; write-сценарий создания кандидата, CV или взаимодействия не описан.
- [web-app.properties](/Users/alekseyananyev/StudioProjects/hunttech_recruiting/modules/web/src/com/company/hunttech/web-app.properties) включает CUBA REST API v2 на `/api`, OAuth2 password/refresh_token, закрытый anonymous-доступ и `GENERIC_UI` security scope. Для записи нужен отдельный клиент/роль/scope, а не безусловное расширение generic CRUD.

### Модель данных и обязательные связи

- [JobCandidate.java](/Users/alekseyananyev/StudioProjects/hunttech_recruiting/modules/global/src/com/company/hunttech/entity/JobCandidate.java) требует `firstName` и `secondName`; хранит контакты, город, должность, компанию, статус и композиции резюме/взаимодействий.
- [CandidateCV.java](/Users/alekseyananyev/StudioProjects/hunttech_recruiting/modules/global/src/com/company/hunttech/entity/CandidateCV.java) требует владельца-кандидата (`CANDIDATE_ID`) и `datePost`; поддерживает текст резюме, письмо, ссылки, связанные вакансии/должности, владельца и файловые поля.
- [IteractionList.java](/Users/alekseyananyev/StudioProjects/hunttech_recruiting/modules/global/src/com/company/hunttech/entity/IteractionList.java) требует кандидата, вакансии и рекрутера (`CANDIDATE_ID`, `VACANCY_ID`, `RECRUTIER_ID`); содержит дату, тип, способ коммуникации, комментарий, рейтинг и календарные поля.
- В [JobCandidateEdit_Spec.md](/Users/alekseyananyev/StudioProjects/hunttech_recruiting/docs/screens/job-candidate/JobCandidateEdit_Spec.md) зафиксировано, что при сохранении нового кандидата проверяется дубль по ФИО + город + должность, нормализуются ФИО/Telegram и автоматически создаётся взаимодействие «Новый контакт». API должен явно определить, повторяет ли он эту UI-логику или принимает внешнюю команду на создание конкретного взаимодействия.
- В [hunttech_JobCandidate.edit_Spec.md](/Users/alekseyananyev/StudioProjects/hunttech_recruiting/docs/screens/job-candidate/hunttech_JobCandidate.edit_Spec.md) описаны связи CV, взаимодействий и поведение нового кандидата; API не должен обходить обязательные связи или создавать промежуточные orphan-записи.

## Предлагаемый целевой контракт для аналитики

Предварительно нужен versioned application endpoint, например `POST /api/integrations/v1/candidates`:

- вход содержит `externalRequestId`/idempotency key, внешний ID кандидата, данные карточки, массив CV и массив взаимодействий;
- каждое CV связывается с кандидатом и при необходимости с существующей вакансией/должностью;
- каждое взаимодействие связывается с кандидатом, существующей вакансией и допустимым пользователем-рекрутером; нельзя принимать произвольный `RECRUTIER_ID` без проверки прав;
- файлы передаются по отдельному безопасному контракту (multipart или предварительно загруженный file token), без включения секретов и без предположения, что текущий FileStorage уже BLOB-хранилище;
- ответ возвращает IDs кандидата, CV и взаимодействий, статус операции и correlation ID;
- повтор с тем же idempotency key идемпотентен; повтор с тем же внешним ID обрабатывается по согласованной политике `reuse/update/reject`;
- при пакетной операции ошибка в обязательной связи или валидации не оставляет частично созданный комплект, либо API явно поддерживает режим частичного результата с отдельным контрактом.

Конкретный endpoint, DTO, порядок создания, дедупликация и режим частичной обработки должны быть утверждены аналитикой.

## Ожидаемый результат

Авторизованный внешний клиент создаёт или безопасно переиспользует карточку кандидата, сохраняет связанные резюме и взаимодействия, а HRM получает целостный граф данных: `JobCandidate → CandidateCV` и `JobCandidate → IteractionList → OpenPosition/ExtUser`.

## Предварительный план проработки

1. Согласовать источник и внешний ключ кандидата, частоту и размер пакетов, требования к обновлению и удалению.
2. Описать DTO и mapping всех обязательных полей, включая enum/справочники и timezone дат взаимодействий.
3. Определить дедупликацию: внешний ID как основной ключ, затем правила HRM по ФИО + городу + должности и контакты; запретить неявное слияние без решения.
4. Определить политику резюме: текст, ссылки, файлы, оригинал/обработанная версия, лимиты, MIME-типы и связь с вакансией.
5. Определить, создаётся ли автоматически «Новый контакт» и как сопоставляется рекрутер для импортированных взаимодействий.
6. Спроектировать core application service с одной транзакцией, DTO-валидацией, idempotency и аудитом; generic entity CRUD не использовать как внешний бизнес-контракт.
7. Реализовать OAuth2 client/roles/scope, rate limit, correlation ID и безопасное логирование без CV-персональных данных сверх необходимого.
8. Добавить тесты: карточка без обязательных полей, CV без кандидата/даты, взаимодействие без вакансии/рекрутера, дубль, повтор запроса, rollback, файл резюме и успешный полный пакет.
9. Опубликовать versioned API-документацию с примерами, ошибками и политикой совместимости.

## Предварительные критерии готовности

- внешний клиент может создать кандидата и получить его стабильный внешний/внутренний ID;
- одно или несколько резюме сохраняются с обязательной связью на кандидата;
- взаимодействия сохраняются только с валидными вакансией и рекрутером;
- повтор доставки не создаёт дубликаты согласно утверждённой политике;
- невалидный пакет не оставляет orphan CV/interaction или возвращает согласованный частичный результат;
- файлы и текст CV ограничены по размеру/типу и не раскрываются в логах;
- права внешнего клиента ограничены выделенным scope и не позволяют произвольный доступ к другим данным;
- существующий UI и умная загрузка кандидата продолжают работать без изменения бизнес-правил;
- API, сервис и миграции/индексы при необходимости покрыты тестами и документированы.

## Открытые вопросы и допущения

- Вопрос: внешний клиент передаёт одного кандидата за запрос или пакет кандидатов?
- Вопрос: нужен ли только create или также обновление существующей карточки, добавление новой версии CV и новых взаимодействий?
- Вопрос: какой внешний идентификатор является источником истины и как поступать с дублем по телефону/email/ФИО?
- Вопрос: какие поля CV передаются — текст, URL, оригинальный файл, обработанный файл, сопроводительное письмо?
- Вопрос: взаимодействие всегда относится к существующей вакансии и рекрутеру или API должно уметь создать их в рамках одной интеграции с BL-2026-025?
- Вопрос: создаётся ли системное взаимодействие «Новый контакт» автоматически, как в UI, или только явно переданные записи?
- Вопрос: допустима ли частичная обработка пакета и как внешний клиент узнаёт о результатах отдельных элементов?
- Допущение: интеграция должна проходить через сервисный слой и транзакцию, а не напрямую записывать сущности generic REST CRUD.

## История

- 2026-09-22 — карточка создана как FEATURE; подтверждены обязательные связи JobCandidate/CandidateCV/IteractionList и отсутствие текущего write-контракта в REST queries.
