# Отчёт Hermes-1: фикс OOM тестовых экранов кандидатов (после PR #143)

Дата: 2026-08-15 · Ветка: master (0.72) · По протоколу 3 агентов

## Контекст

PR #143 (agent/antigravity-dev, «исправление типов действий и добавление тестовых
экранов кандидатов») смержен (b411e70e) и задеплоен. Smoke выявил:
- «Тест 2» — `RemoteException: Java heap space` (loader без лимита, 11 537 кандидатов);
- окна «Файл … не найден» (CubaImage) — локальный FileStorage пуст;
- «Тест 3» — метрики канбана «Всего кандидатов 0».

Поручение пользователя: «Сделай сам. Добейся открытия всех экранов».

## Выполнено

1. `fix(ui)` 9d868b99 — `maxResults="200"` во всех 6 `job-candidate-test*-browse.xml`
   (паттерн как в `open-position-browse.xml`).
2. Data fix (dev-БД hunttech, не схема): обнулены битые ссылки на файлы при пустом
   `fileStorage/` — `hunttech_job_candidate.file_image_face` (1816),
   `sec_user.official_photo_id/user_avatar_id/image_id` (76),
   `hunttech_user_settings.file_image_face` (0). Fallback-аватары вместо ошибок.
3. `fix(ui)` 72d939fb — канбан Тест 3: сборка карточек и метрик перенесена из
   `BeforeShowEvent` в `@Subscribe` на `CollectionChangeEvent` (REFRESH) контейнера
   `jobCandidatesDc` — до фикса экран показывал 0 кандидатов, т.к. BeforeShow
   срабатывает до загрузки данных.
4. Проверка view integrity: все getter'ы тестовых контроллеров
   (`fileImageFace`, `fullName`, `personPosition`, `cityOfResidence`, `currentCompany`,
   `iteractionList`, ...) покрыты view `jobCandidate-view` — UNFETCHED нет.

## CDP smoke (вход admin, открытие каждого пункта меню)

| Экран | Окно | Ошибки до | Ошибки после | Итог |
|-------|------|-----------|--------------|------|
| Тест 1: Split-View (Halo) | #main/0 | — | нет | PASS |
| Тест 2: High-Density DataGrid | #main/1 | heap space | нет | PASS |
| Тест 3: Kanban & Метрики | #main/2 | метрики 0 | нет (после 72d939fb) | PASS |
| Тест 4: Executive Card-Grid | #main/3 | — | нет | PASS |
| Тест 5: Row Expansion | #main/4 | — | нет | PASS |

## Backlog

- Восстановление `fileStorage/` с прода — для полноценных аватаров (по запросу).
- Канбан Тест 3 использует демо-распределение `i % 4` по колонкам — не реальные стадии.
- Вопрос: нужны ли 5 пунктов «Тест 1–5» в общем меню (сейчас в разделе «Рекрутинг»).
- PR от Antigravity не найден: открытых PR нет (проверено gh CLI + GitHub API + remote-ветки).
