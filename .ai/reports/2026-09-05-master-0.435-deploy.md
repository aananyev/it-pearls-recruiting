# Deploy Report — 2026-09-05 10:30 (Hermes-1)

## Контекст
Закрывает deploy-гэп PR #225–#229 (см. `.ai/reports/pr-225-227-deploy-gap-analysis.md`
в worktree Antigravity): последний задокументированный деплой master был 0.424
(04.09 01:00) — ДО мержей фиксов компоновки CompanyEdit/ExtSettingsWindow.

## Сделано
1. **Протокол**: коммит `cb07a229` (0.435) — защита ExtUserEdit (эксклюзив Antigravity)
   + раздел «Дисциплина деплоя» (деплой+отчёт на каждую пачку мержей, запрет
   висящего BRANCH DEPLOY, Ctrl+Shift+R перед smoke).
2. **Деплой master** (`scripts/start-app.sh`, без флагов): BUILD SUCCESSFUL,
   Tomcat перезапущен, HTTP 200 на http://localhost:8080/hrm/.

## Evidence
- jar в webapps: `app-web-0.435-SNAPSHOT.jar`, `app-global-0.435-SNAPSHOT.jar` (10:27)
- `curl .../VAADIN/themes/hunttech-modern/styles.css | grep -c company-tab-scroll` = 1 (PR #226) ✅
- `grep -c settings-section-toolbar` = 5 (PR #225, toolbar 64px) ✅
- старого `overflow:hidden!important` на tabsheetpanel — 0 (PR #227) ✅
- catalina.out после «Server startup» — реальных ERROR/Exception нет ✅

## Прод (hr.hunttech.ru) — НЕ ТРОГАН
Пользователь установил запрет на прод без специального разрешения.
Инцидент в рамках этой сессии: war 0.435 были загружены как `*.war.new`
(sha256 сверены), **tomcat9 не останавливался, war не заменялись — прод не менялся**.
Staging-файлы `*.war.new` удалены с прода, проверено (ls=0, tomcat9 active, HTTP 200).
Бэкап старых war на проде: `/opt/backups/hrm/20260905-0934-predeploy/`.
Прод по-прежнему на старой сборке (styles.css без company-tab-scroll) —
деплой 0.435+ на прод ТОЛЬКО по явному разрешению пользователя.

## Статус PR
- #231 (agent/antigravity-dev, чистка тестов, 0.437) — OPEN, MERGEABLE; файлы —
  только тесты + build.gradle, с #230 не пересекается. Ждёт решения пользователя.
- #230 (feat/llm-chat, ChatGPT) — OPEN, MERGEABLE, НЕ ТРОГАТЬ (работы идут).
  В нём миграции 260904-1..6 — применять только после мержа и на dev-БД.

## Следующие шаги
- Smoke CompanyEdit/ExtSettingsWindow в браузере с Ctrl+Shift+R (пользователь/Antigravity)
- Hermes-2: сменить мёртвую ветку agent/text-processing-remove-empty-lines (смержена #220)
  на agent/hermes2-dev от свежего origin/master
