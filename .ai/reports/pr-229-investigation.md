# Отчёт: Расследование отсутствия PR #229 в итоговой сборке

**Дата:** 2026-09-04
**Автор:** hrm-dev (Antigravity)
**Статус:** Завершено

---

## 1. Резюме

**PR #229 (commit 2fc96ead) УСПЕШНО ЗАМЕРДЖЕН в master 2026-09-04 22:16:48 (+04).** Версия в `build.gradle` обновлена с `0.433` → `0.434` (commit 52fb8058).

**Проблема:** Пользователь не видит изменений PR #229 в *текущем деплое* — потому что **последний успешный master-деплой (не branch-deploy) выполнен 2026-09-04 23:18:23**, и он развернул версию **0.434-SNAPSHOT** (master), а не ветку разработчика.

В worktree `hrm-antigravity` уже версия **0.437** (PR #231 — OPEN, но изменения уже в ветке). Branch-деплои с worktree Antigravity (22:23, 22:51, 23:18 — нет, 22:23 и 22:51 — branch, 23:18 — master) перезаписывали общий Tomcat версией 0.437.

---

## 2. Хронология ключевых событий

| Время (+04) | Событие | Ветка/Worktree | Версия | Тип деплоя |
|-------------|---------|----------------|--------|------------|
| 17:58:09 | Commit 6792209e: протокол защиты форм | master (local) | 0.433 | — |
| 21:03:55 | Commit 52fb8058: фикс теста + **версия 0.434** | agent/antigravity-dev | 0.434 | — |
| 22:16:48 | **Merge PR #229** (Hermes-1) | **master ← agent/antigravity-dev** | **0.434** | — |
| 22:16:50 | Deploy start (PID=71029) | **hrm-antigravity (wt)** | **0.437** | **BRANCH DEPLOY** |
| 22:23:31 | **BRANCH DEPLOY done** HTTP 200 | hrm-antigravity | 0.437 | Branch |
| 22:45:34 | Deploy start (PID=75099) | master (common copy) | 0.434 | **Master deploy** |
| 22:51:50 | **Deploy done** HTTP 200 | master | 0.434 | Master |
| 23:12:34 | Deploy start (PID=81730) | master (common copy) | 0.434 | **Master deploy** |
| 23:18:23 | **Deploy done** HTTP 200 | master | 0.434 | Master |

---

## 3. Текущее состояние деплоя

```bash
# JAR-файлы в общем Tomcat (deploy/tomcat/webapps/hrm/WEB-INF/lib/):
app-global-0.434-SNAPSHOT.jar   # ← master версия 0.434 (isSnapshot=true)
app-web-0.434-SNAPSHOT.jar
app-web-toolkit-0.434-SNAPSHOT-client.jar
...
```

**Последний master-деплой:** 2026-09-04 23:18:23 (PID=81730) — **версия 0.434-SNAPSHOT** (то есть изменения PR #229 **включены**).

**Worktree hrm-antigravity:** уже на версии **0.437** (commit 47b2e759 — перенос очистки тестов от Hermes-1). PR #231 открыт, но не замерджен.

---

## 4. Почему может казаться, что PR #229 «не попал в сборку»

1. **Branch-деплой 22:23:31** развернул worktree Antigravity (v0.437) — если проверяли UI сразу после этого, видели версию 0.437 (с изменениями PR #231, но без мерджа в master).
2. **Master-деплой 22:51:50 и 23:18:23** развернули master (v0.434) — PR #229 там есть.
3. **Версионирование:** `isSnapshot = true` → все артефакты имеют суффикс `-SNAPSHOT` (0.434-SNAPSHOT, 0.437-SNAPSHOT). Числовая часть версии **увеличилась с 0.433 → 0.434** в PR #229.

---

## 5. Конфликты shared-файлов (включая build.gradle)

### Правило протокола (three-agent-git-protocol-2026-08-15.md):
> **Конфликты shared-файлов резолвит автор ветки, обе стороны; version build.gradle — только pre-commit hook.**

### Что произошло в PR #229:
- Commit 6792209e (master): версия 0.432 → 0.433
- Commit 52fb8058 (agent/antigravity-dev): версия 0.433 → 0.434 + фикс теста
- **Merge commit 2fc96ead**: обои изменения version (0.433 → 0.434) + тест — **конфликт разрешён в пользу автора ветки (Antigravity)**

### Текущий конфликт (PR #231 — OPEN):
- master: 0.434
- agent/antigravity-dev: 0.437 (бамп версии в коммите 47b2e759)
- При мердже PR #231 pre-commit hook должен разрешить версию (старшая = 0.437)

---

## 6. Варианты решения / рекомендации

### Вариант А: Проверить текущий master-деплой (рекомендую)
```bash
# В общей копии — проверить версию в запущенном приложении
curl -s http://localhost:8080/hrm/ | grep -i version
# или зайти в UI → About / логи запуска
```
**Ожидаемый результат:** версия 0.434-SNAPSHOT (PR #229 включён).

### Вариант Б: Если нужен PR #229 именно в branch-деплое для тестирования
Branch-деплой всегда берёт **текущее состояние worktree**. В hrm-antigravity сейчас 0.437 (PR #231). Чтобы протестировать именно PR #229 без PR #231:
```bash
# В worktree hrm-antigravity:
git checkout 2fc96ead  # или 52fb8058 — HEAD PR #229
bash ../hunttech_recruiting/scripts/start-app.sh --branch "$PWD"
```

### Вариант В: Дождаться мерджа PR #231 (версия 0.437)
PR #231 проходит тесты (619 тестов, 0 падений). После мерджа master станет 0.437, и следующий master-деплой развернёт его.

### Вариант Г: Принудительный master-деплой (если Tomcat «застрял» на branch-версии)
```bash
# В общей копии (../hunttech_recruiting):
bash scripts/start-app.sh --force
# Guard'ы: master branch, чистая копия, нет новых миграций
```
Это перезапишет Tomcat версией 0.434 (master) и вернёт PR #229 в продакшен-окружение.

---

## 7. Вывод

**PR #229 В МАСТЕРЕ И В ПОСЛЕДНЕМ MASTER-ДЕПЛОЕ (23:18:23).** Версия 0.434-SNAPSHOT.

Если в UI видите другую версию — проверьте, не застрял ли Tomcat на branch-деплое (0.437). Запустите Вариант Г для восстановления master-версии.

---

## 8. Субагенты

В расследовании участвовали:
- **Основной агент (hrm-dev)**: анализ git-истории, логов деплоя, версий JAR, PR-статусов
- Никакие дополнительные субагенты (analyst, qa, ui/ux) не требовались — задача чисто диагностическая