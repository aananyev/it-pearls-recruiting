# Проверка календарей пользователя alan и автоматизированные тесты

**Дата:** 2026-09-22
**Статус:** WAITING_FOR_HERMES
**BASE_SHA:** `0c1885abe4e677a3895a3d7b833956873f1e6132`
**Ветка:** `agent/alan-calendar-connection-tests`

## Цель

Проверить архитектуру и фактическую доступность календарных интеграций пользователя `alan`, не изменяя production-данные, и добавить автоматизированную проверку CalDAV CRUD, ошибок авторизации и сети.

## Разрешённая область

- read-only диагностика TEST/PRODUCTION;
- `YandexIntegrationServiceBean` — минимальное исправление достоверности диагностики;
- автоматизированные тесты календарного подключения;
- документация интеграции и итоговый диагностический отчёт.

## Запрещённая область

- изменение реальных событий `alan`;
- запись в production БД или календарь;
- деплой и миграции;
- публикация OAuth/IAM-токенов, паролей и иных секретов;
- изменение календарной бизнес-логики вне выявленного дефекта диагностики.

## Зафиксированный дефект

`CAL-2026-001`, P1: `testConnection(..., "CALENDAR")` использует discovery с fallback. При HTTP 401/403 или сетевой ошибке discovery возвращает синтетический календарь, после чего диагностика устанавливает `calendarConnected=true` и сообщает об успешном подключении.

## Критерии приёмки

- строгая диагностика отличает успешный CalDAV discovery от 401/403/5xx и сетевой ошибки;
- штатный fallback discovery рабочих сценариев сохранён;
- тесты покрывают отсутствие токена, ошибку авторизации, сетевой сбой и mock-CalDAV create/read/update/delete с cleanup;
- токены отсутствуют в тестовых данных, логах и отчёте;
- документация и отчёт синхронизированы;
- production не изменён.

## Чекпоинты

- [x] Изучены HEAD, открытые PR, `.ai/tasks`, инструкции и отчёты.
- [x] Найдены сущности, сервис, экраны, миграции и существующие тесты.
- [x] Выполнена безопасная проверка сетевой доступности без аутентификации и записи.
- [x] Реализовано минимальное исправление `CAL-2026-001` и namespace-fix `CAL-2026-002`.
- [x] Добавлены автоматизированные тесты.
- [x] Обновлена документация и подготовлен итоговый отчёт.
- [x] `git diff --check` пройден; `ocr` в окружении отсутствует.
- [ ] Hermes проверил точный итоговый SHA.
- [ ] Создан PR.
