# Отчёт ручной CDP-проверки изменений за последние 24 часа

Дата: 2026-09-22  
Локальный SHA: `4ff8e22dd`  
База сравнения: `origin/master` (`c13e70900`)  
Окружение: LOCAL, `http://localhost:8080/hrm/`, production не использовался.

## Что проверено

- PR #271 и PR #272 приняты в локальную ветку; локальный deployment выполнен после применения миграций.
- Вход и роль проверены через CDP для `alan` и `eliberman`.
- CandidateVacancyMatch проверен при 768×600, 900×650 и 1100×780: корневой контейнер помещается во viewport, длинные кнопки переносят подписи, наложений нет.
- Открыты и проверены smoke-сценарии Kanban/worklist, вакансии и «Умная AI-загрузка», InteractionList, AI connections, AI functions, background skills monitor, пользовательские AI-замещения, чат AI и корпоративные Яндекс-календари.
- В календарном экране загружены две существующие записи, доступны действия создания, изменения, удаления и обновления.
- При отключённом локальном scheduler AI-подбор корректно перешёл в предусмотренный эвристический fallback с сообщением о недоступности AI-сервиса.
- OCR-проверка после исправлений выполнена для изменений от `origin/master` до `4ff8e22dd`.

## Исправленные риски

- В CandidateVacancyMatch устранено наложение узких action-кнопок на малом viewport.
- В MarkdownRenderer продолжение таблицы теперь требует совпадающего внешнего pipe-стиля; строка с другим стилем остаётся обычным текстом. Одноколоночные таблицы сохранены отдельным тестом.
- Ошибки некорректного изображения отделены от внутренних ошибок обработки и storage; внутренний сбой больше не маскируется сообщением о неверном формате.

## Ограничения текущего прогона

- Полные create/update/delete сценарии календаря и граничные image-fixtures (20 MiB, 25 MP, повреждённый файл) не выполнялись в CDP из-за отсутствия безопасных тестовых данных в локальной базе.
- Реальный внешний AI endpoint не вызывался; проверен безопасный локальный fallback при отключённом scheduler.

Эти ограничения оформляются как P2 backlog-задачи для отдельного прогона с подготовленными fixtures. Критических дефектов по выполненным сценариям не выявлено.

## Результат

Для выполненных сценариев: **PASS**. Production не изменялся. Незакоммиченные локальные артефакты и миграция `260920-4-fixSkillsExtractBackgroundBinding.sql` сохранены и в deployment не включались.
