# Слияние изменений за последние сутки

## Цель

Объединить незлитые изменения, найденные 2026-09-22, в `master`, сохранить связанный backlog, миграции и отчёт ручной проверки.

## Исходное состояние

- `BASE_SHA`: `8059343f785735ca5c5eff90b2f2cbc36be74eb3` (master после merge PR #281).
- PR #281 (`agent/backlog-bl-2026-020`) объединён в `master`.
- В интеграцию включены ветки `agent/backlog-project-manager`, `agent/bl-2026-020-provider-model-clean`, `agent/yandex-calendar-alan-diagnostics` и локальная `agent/fix-ocr-risks-2026-09-22`.
- В рабочей копии исходной ветки остаются незакомиченные пользовательские файлы и локальная SQL-миграция `260920-4-fixSkillsExtractBackgroundBinding.sql`; они сохранены и не включены в это слияние.

## Объём

- Добавлены материалы backlog manager и статус BL-2026-019.
- Объединены сохранение фактических AI provider/model и `EXECUTION_SOURCE`, тесты и документация.
- Объединена диагностика Яндекс Календаря с сохранением текущего strict-режима только для диагностики; обычное discovery сохраняет прежний fallback.
- Объединены OCR-исправления загрузки изображений, Markdown-таблиц и адаптивной компоновки CandidateVacancyMatch.
- В XML и `@DialogMode` синхронизированы viewport-размеры диалога; разделитель Markdown-таблицы требует pipe; стиль разделителя может отличаться от стиля строк таблицы.
- Ошибки и фокус поля времени сохраняют красную индикацию во всех семи темах.
- Миграция `260922-1-addExecutionSourceToCandidateCvSkillAnalysis` уже зарегистрирована в актуальном changelog; дублирующая регистрация не добавлена.

## Статус и проверки

Статус: `IN_PROGRESS`.

- OCR-ревью провело проверку provider-model, Yandex Calendar и OCR-risk веток. Подтверждённые риски исправлены; низкоприоритетный совет использовать Apache `StringUtils` отклонён как рекомендация по стилю.
- Профильные unit/contract tests прошли до последнего изменения SCSS. Тест `ScreenViewIntegrityTest` не стартовал: требуется `HUNTTECH_LOCAL_DB_PASSWORD`; подключение к LOCAL БД не выполнялось.
- `:app-web:buildScssThemes` прошёл до последнего изменения SCSS. Повторная проверка обязательна.
- Production и базы данных не изменялись.

## Следующий шаг

Завершить повторные проверки, обновить статус и открыть интеграционный PR в `master`.
