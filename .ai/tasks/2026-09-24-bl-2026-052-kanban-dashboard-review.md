# Задача BL-2026-052: Канбан-дашборд кандидата (Статус: НА ПРОВЕРКУ)

- **Дата**: 2026-09-24
- **Задача**: BL-2026-052 (Rich UI дашборда Kanban, исправление фильтрации кандидатов рекрутера, скроллер и эргономика)
- **Исполнитель**: Antigravity (Главный разработчик / Tech Lead)
- **Статус**: **REVIEW (НА ПРОВЕРКУ)**
- **Ветка**: `master` / `agent/antigravity-dev`
- **Коммиты**: `07224171f`

## Состав реализованных работ
1. **Поддержка активностей рекрутера и fallback-выборка**:
   - `userSession.getCurrentOrSubstitutedUser()`, исправление отсечения взаимодействий без вакансии (`x.vacancy is null`).
   - Расширение периода по умолчанию до 90 дней (233 активных кандидата у Либерман).
2. **Презентационный Rich UI карточек по Эскизу 3**:
   - Круглый фото-аватар 42px с инициалами при отсутствии фото (`WebOvaFallbackImage`).
   - Должность кандидата (`personPosition`).
   - Золотая звезда рейтинга ★ и город 📍.
   - Бейдж вакансии `💼`, статус и дата взаимодействия.
   - Интерактивная кнопка «Карточка профиля» с прямым открытием экрана редактирования кандидата.
3. **Вертикальный скроллер для карточек**:
   - Для `ScrollBoxLayout` в `RecruiterCandidateKanbanWidget.java` активирован вертикальный скролл (`scrollBox.setScrollBarPolicy(VERTICAL)`).
   - В SCSS для колонок задан `overflow-y: auto !important` с кастомным тонким скроллбаром. Все нижние карточки в колонках прокручиваются и полностью доступны.
4. **Нормализация интервалов и эргономика шапки**:
   - На корневой layout добавлен атрибут `expand="kanbanScrollBox"`. Лишнее вертикальное пространство устранено.
   - Поля шапки подогнаны по размеру контента (`width="AUTO"`, компактные радио-кнопки 30 дн. / 90 дн. / Всё время, кнопка обновления 28px).
5. **Спецификация и документация**:
   - Оформлен детальный документ экранной компоновки и бизнес-логики: [RecruiterCandidateKanbanWidget_Spec.md](file:///Users/alekseyananyev/StudioProjects/hrm-antigravity/docs/screens/recruiter-dashboard/RecruiterCandidateKanbanWidget_Spec.md).
6. **Кросс-темовая синхронизация и тесты**:
   - Все стили синхронизированы в 7 темах оформления (`halo`, `havana`, `helium`, `hover`, `hunttech-modern`, `hunttech-modern-dark`, `hunttech-modern-light`).
   - Контрактный тест `RecruiterDashboardsContractTest` — **100% PASS**.
   - Приложение задеплоено, HTTP 200 на `http://localhost:8080/hrm/`.
