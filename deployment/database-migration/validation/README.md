# Validation

На первом этапе здесь фиксируются будущие validation checks.

Минимальные будущие проверки:

- row counts;
- FK orphan checks;
- sequence values;
- CUBA security consistency;
- `sys_file` and file storage references;
- application smoke tests.
