# CandidateVacancyMatchScreen — диалог «Подобрать вакансию»

## Business & Context Intro

### Назначение и бизнес-смысл (What & Why)

Диалог помогает рекрутеру подобрать кандидату подходящие открытые вакансии. Перед сопоставлением экран показывает актуализацию навыков кандидата, затем отображает фактический прогресс отбора и AI-анализа. Результат ранжируется по соответствию требований вакансии навыкам и опыту кандидата; решение о дальнейшей работе принимает рекрутер.

### UI Context & Navigation

Экран `hunttech_CandidateVacancyMatch` открывается кнопкой `findSuitableVacancyBtn` с подписью «Подобрать вакансию» из карточки кандидата. Это модальный рабочий диалог: верхняя зона сохраняет контекст кандидата, центральная область содержит таблицу результатов и прокручиваемую панель деталей, кнопка «Закрыть» возвращает пользователя в исходную форму.

### Behavior Summary

- открытие диалога → сравнивается дата последнего изменения резюме с самой поздней датой создания или изменения навыков → AI-актуализация запускается, только если резюме новее либо навыков нет; результат фиксации показывается отдельно от прогресса подбора;
- запуск из реестра вакансий → используется режим «вакансия → кандидаты»; если пул кандидатов с распознанным текстом резюме пуст, берутся незаблокированные кандидаты и AI получает профиль и сохранённые навыки с явной пометкой об отсутствии читаемого резюме;
- запуск подбора → выбираются только вакансии `OpenPosition.openClose != true` → экран показывает три этапа, процент выполнения задачи, текущую фазу и ETA;
- завершение AI-пакета → контроллер получает snapshot прогресса → обновляет determinate progress bar без блокировки UI;
- выбор строки результата → справа показываются AI score, подтверждения, пробелы и риски → операционный процент выполнения при этом не меняется;
- действие рекрутера → выполняется существующий controller action → component ID и семантика действий сохранены.

## 1. Точка вызова и контекст (Invocation & Context)

| Параметр | Значение |
|---|---|
| `@UiController` | `hunttech_CandidateVacancyMatch` |
| Java | `CandidateVacancyMatchScreen.java` |
| XML | `candidate-vacancy-match-screen.xml` |
| Тип | modal `Screen`, resizable |
| Целевая геометрия | Полноэкранный modal, адаптивный к viewport: `@DialogMode(width = "95%", height = "90%", resizable = true)`; эталон 1100×780 px |
| focus | `matchesTable` |

## 2. Связь с моделью данных (Data & Entity Binding)

`matchesDc` хранит `CandidateVacancyMatchItem`; `matchesTable` остаётся связан с контейнером без изменения колонок. Данные прогресса не входят в entity и поступают из remote-safe snapshot сервиса: `operationId`, фаза, текстовый статус, обработанные/всего вакансии, завершённые/всего AI-пакеты, процент и ETA.

Timer facet `analysisProgressTimer` имеет интервал 1000 мс, `autostart=false`, `repeating=true`. Контроллер запускает его только на время активной операции и останавливает после завершения, ошибки или закрытия экрана.

## 3. Иерархия и взаимосвязь форм (Form Hierarchy)

| Связь | Экран | Переход |
|---|---|---|
| Родитель | карточка кандидата | `findSuitableVacancyBtn` |
| Дочерний | карточка выбранной вакансии | `openEntityBtn` |
| Дочерний | создание взаимодействия | `createInteractionBtn` / `quickInteractionBtn` |
| Дочерний | помещение в кадровый резерв | `addToReserveBtn` / `quickAddToReserveBtn` |
| Дочерний | AI draft предложения | `outreachDraftBtn` / `quickOutreachBtn` |
| Дочерний | диалог отказа | `rejectBtn` / `quickRejectBtn` |

## 4. Модель поведения и интерактивность (Behavior Model)

### 4.1 Прогресс операции

Лента содержит три бизнес-этапа: «Навыки» → «Открытые вакансии» → «AI-сопоставление». Во время обновления навыков полоса показывает активность этапа, процент остаётся 0% до завершения подготовки. После загрузки вакансий прогресс стартует с 15%; далее процент растёт по числу реально обработанных вакансий и завершается на 100% после итоговой сборки отчёта. `analysisPercentLabel` показывает процент выполнения задачи, он не является AI score соответствия. `analysisPhaseLabel` и `statusLabel` объясняют текущую работу, `analysisEtaLabel` показывает прогноз по реальному времени завершённых пакетов. Пока скорость ещё не измерена, экран показывает прошедшее время и сообщает, что оценка ETA рассчитывается.

### 4.2 Статус навыков

`skillRefreshStatusBox` и `skillRefreshStatusLabel` показывают самостоятельный итог проверки: актуальны, обновлены и зафиксированы, пропущены либо ошибка. Актуальность определяется сравнением даты последнего изменения CV с максимальной датой создания/изменения записей навыков. AI запускается только при отсутствии навыков или когда CV новее. Этот статус не используется как процент AI-анализа.

### 4.3 Результаты и детали

Левая панель содержит заголовок, количество совпадений, действия, фильтр решения рекрутера (`decisionFilter`), чекбокс «Не выводить нерелевантных» (`hideIrrelevantCheckBox`) и таблицу. При включении чекбокса из таблицы исключаются позиции, где AI-вердикт определен как «Не рекомендуется» / «NOT_RECOMMENDED» или AI score < 45 (нерелевантные). Правая панель показывает действия по выбранной строке, решение рекрутера, детализацию AI score, совпадающие навыки, пробелы, риски и заключение; область деталей прокручивается внутри карточки. `scoresBox` визуально отделён от progress panel.

### 4.4 Vacancy-to-candidates: no-CV и ошибки

В режиме, открытом из sidebar реестра вакансий, сервис сначала использует кандидатов с читаемым CV; если таких кандидатов нет, сохраняется предусмотренный fallback на незаблокированных кандидатов. Для кандидата без CV в AI-контекст передаются профиль, сохранённые навыки и текстовая пометка «Резюме с распознанным текстом отсутствует»; сервис не обращается к первому элементу пустого списка CV.

Запуск получает operation ID, а отдельный AI-вызов — request ID. Серверные сообщения связывают их с вакансией, UUID кандидата, фазой обработки и типом исключения; лог не содержит имени кандидата, CV-текста, полного промпта/ответа, токенов или ключей. Ошибка подготовки одного кандидата пропускает его и не прерывает остальные элементы batch. Экран показывает сообщение об ошибке/частичном результате с кодом обращения, сбрасывает busy-индикатор и снова разрешает кнопку «Повторить». Если AI-ответ не дал разбираемого совпадения, UI явно отмечает rule-based оценку как предварительную; пустой пул кандидатов остаётся отдельным сообщением.

## 5. Логика управляющих элементов (Actions & Buttons Logic)

Существующие действия и их component ID сохранены: `refreshAnalysisBtn`, `closeBtn`, `takeIntoWorkBtn`, `createInteractionBtn`, `outreachDraftBtn`, `openEntityBtn`, `bulkTakeIntoWorkBtn`, `postponeBtn`, `rejectBtn` и пять `quick*` кнопок. Изменена только группировка: основные действия располагаются в первой строке, решения и открытие карточки — во второй; быстрые действия справа собраны в две компактные колонки.

Новые presentation-компоненты для controller wiring:

| ID | Контракт |
|---|---|
| `analysisProgressTimer` | polling snapshot раз в 1000 мс во время активной операции |
| `skillRefreshStatusLabel` | итог проверки и фиксации навыков кандидата |
| `analysisPhaseLabel` | человекочитаемая текущая фаза |
| `analysisStepsLabel` | позиция в ленте, например «Шаг 2 из 3» |
| `analysisPercentLabel` | процент выполнения задачи: `0%` до завершения проверки навыков, затем растёт по обработанным вакансиям до `100%` |
| `analysisEtaLabel` | оценка оставшегося времени либо «рассчитываем» |
| `analysisProgressBar` | показывает активность обновления навыков и determinate прогресс по AI-пакетам; существующий ID сохранён |
| `statusLabel` | подробный текст текущей операции, существующий ID сохранён |
| `progressStepSkillsLabel` | visual state этапа навыков |
| `progressStepVacanciesLabel` | visual state отбора открытых вакансий |
| `progressStepAiLabel` | visual state AI-сопоставления |
| `hideIrrelevantCheckBox` | чекбокс фильтрации нерелевантных записей («Не выводить нерелевантных») с вердиктом «Не рекомендуется» / score < 45 |

## 6. Визуальная компоновка элементов (Visual Layout Schema)

```text
┌ Контекст кандидата: ФИО · роль · город                         Повторить · Закрыть ┐
├ Навыки кандидата · самостоятельный статус проверки/фиксации                    ┤
├ Текущая фаза                                      Шаг N из 3            NN% ┤
│ progress bar                                                                    │
│ [1 Навыки] [2 Открытые вакансии] [3 AI-сопоставление]             ETA          │
├──────────────────────────────────────┬─────────────────────────────────────────┤
│ Результаты и действия                │ Детали выбранного совпадения (scroll)   │
│ фильтр · таблица                     │ AI score · навыки · пробелы · риски     │
└──────────────────────────────────────┴─────────────────────────────────────────┘
```

Локальный namespace — `candidate-vacancy-match-`. Экран открывается полноэкранным modal и подстраивается под текущий viewport; эталонная геометрия — 1100×780 px. SCSS partial подключён во всех семи темах и содержит только screen-scoped selectors. Корневой layout имеет ровно один expand-компонент — `mainSplit`; шапка, статусы, прогресс и карточка действий имеют auto-height. На ширине выше 1000 px `SplitPanel` сохраняет нативную горизонтальную геометрию CUBA. SCSS не меняет `position`, `width`, `height`, `display` или `flex` общих Vaadin slot/expand-обёрток: размеры верхних строк и таблицы рассчитывает layout manager CUBA.

Ниже 1000 px только сам `mainSplit` и его два непосредственных контейнера переходят в вертикальный стек с минимальной высотой 180 px и прокруткой рабочей области; splitter скрывается. Правила не распространяются на вложенные HBox/VBox, actions, таблицу и data-bound компоненты. Длинные тексты деталей переносятся внутри карточек, таблица остаётся в собственной области, а правая панель прокручивается независимо. Это сохраняет обычный, пустой, длинный и ошибочный результаты при размерах 1100×780, 900×650 и 768×600 и не накапливает геометрию при повторном открытии.

В узком режиме кнопки в строках действий занимают ширину своего слота, допускают перенос подписи и увеличиваются по высоте. Благодаря этому «Взаимодействие», «Предложение (AI)» и другие длинные действия не перекрывают соседние кнопки на ширине 768 px.

## История изменений

| Дата | Изменение |
|---|---|
| 2026-09-24 | Реализовано действие «Поместить в кадровый резерв» (`addToReserveBtn` в строке действий над таблицей и `quickAddToReserveBtn` в сайдбаре быстрых действий). При выборе кандидата в таблице кнопка активируется и открывает диалог `PersonelReserveEdit` с автозаполнением кандидата, должности, вакансии, текущего рекрутера, дат и 30-дневного срока. Аналогичное действие подключено в `JobCandidateReestr`. |
| 2026-09-23 | BL-2026-042: режим vacancy-to-candidates безопасно передаёт профиль и навыки кандидатов без читаемого CV, диагностика связывает попытки с operation/request ID без логирования PII/контекста AI, а экран показывает безопасный статус и код обращения для fallback/частичной ошибки. |
| 2026-09-23 | BL-2026-032: В контроллере `CandidateVacancyMatchScreen.java` исправлена аннотация `@DialogMode`: некорректные единицы `95vw` / `90vh`, вызывавшие исключение `IllegalArgumentException: Invalid size argument: "95vw"` в `com.vaadin.server.SizeWithUnit.parseStringSize`, заменены на валидные процентные значения `95%` / `90%`. Диалог проверен тестом `AllXmlScreensIntegrityTest`. |
| 2026-09-22 | CDP-проверка 768×600 выявила переполнение подписей в строках действий; во всех семи темах кнопки получили ширину слота, перенос текста и auto-height без изменения бизнес-действий. |
| 2026-09-22 | BL-2026-032: удалено глобальное вмешательство в геометрию Vaadin slot/expand-контейнеров, появившееся после merge PR #260; восстановлен нативный расчёт CUBA для обычной ширины, а узкий режим ограничен непосредственными контейнерами `mainSplit`. Добавлен контракт идентичности и локальной области SCSS для семи тем. |
| 2026-09-22 | Исправлен dialogMode: XML и `@DialogMode` используют 95% × 90% (стандартные единицы Vaadin/CUBA вместо неподдерживаемых vw/vh), чтобы диалог оставлял поля окна и не вызывал IllegalArgumentException. |
| 2026-09-22 | Исправлены ID-селекторы Vaadin slots, цепочка высоты expand-контейнеров и ширина flex-строк; все 7 тем синхронизированы. На узкой ширине стек панелей прокручивается целиком, а детали — внутри своей области, чтобы не обрезать заголовок и кнопки в коротком окне. |
| 2026-09-21 | Исправлено наложение элементов диалога: удалён CSS-hack, превращавший горизонтальный SplitPanel в стек с фиксированными высотами 420/360 px; верхние блоки и действия переведены на auto-height и локальный flex-wrap, добавлены `min-width/min-height: 0` и независимая прокрутка таблицы/деталей. |
| 2026-09-21 | Уточнено: навыки актуальны по timestamp CV/CandidateSkill; внешний процент учитывает подготовительный этап и завершённые AI-пакеты |
| 2026-09-21 | Визуальный рефакторинг диалога: компактный контекст кандидата, отдельный статус навыков, лента трёх этапов, determinate процент и ETA, рабочая зона результатов, прокручиваемые детали AI score; добавлен timer contract и локальный SCSS для 7 тем. |
