# OpenPositionEdit — UI Spec

> Форма: редактирование позиции (Edit) · Controller: `hunttech_OpenPosition.edit`
> Дескриптор: `modules/web/src/com/company/hunttech/web/screens/openposition/open-position-edit.xml`
> Контроллер: `OpenPositionEdit.java` · Проект: **HRM HuntTech** (CUBA 7.3)
> Статус раздела «Визуальная компоновка»: обновлён 2026-08-05 (визуальный редизайн по UI-контракту)

---

## Business & Context Intro

### Назначение и бизнес-смысл (What & Why)

Форма — «паспорт» вакансии: рекрутер ведёт всю информацию о позиции от названия и внутреннего ID до схемы оплаты подрядчиков и текстов для кандидатов. Здесь задаются: принадлежность
проекту/компании/городу, тип позиции и грейд, приоритет и дата автозакрытия, вилка зарплаты и зарплата ИП, условия для ресурсера и рекрутера (процент/сумма, НДФЛ), описание на русском и
английском, тестовое задание, памятка к собеседованию, шаблон сопроводительного письма, дерево навыков, файлы, новости и комментарии-рейтинги. Форма интегрирована с BPM-согласованием и
рассылкой уведомлений (email/Telegram) подписчикам при открытии/закрытии позиции.

Визуальный редизайн 2026-08-05 привёл форму к общему UI API Edit-экранов HRM HuntTech
(`HRM_HuntTech_Edit_Screen_Shared_Style_Contract.md`): двухпанельная композиция
«тёмный sidebar → рабочая область», единые `edit-*` / `label-*` stylename, эталонная
label-навигация `IteractionListEdit`, карточки-аккордеоны, responsive-строки полей и
плотные варианты таблиц/редакторов. Изменён **только presentation-слой**: XML-компоновка
и stylename, локальный SCSS, визуальные подписи и документация. Бизнес-логика, entity,
views, loaders, JPQL, actions, invoke, required/visible/enabled и Java-контроллер не изменялись.

### Связи в интерфейсе и Навигация (UI Context & Navigation)

Открывается из `hunttech_OpenPosition.browse` (действия create/edit), из lookup-экранов подбора и из `JobCandidateEdit` (просмотр позиции кандидата). Из формы доступны: окно выбора городов
`hunttech_SelectCitiesLocation` (кнопка «Добавить города»), справочники-просмотры picker-полей (проект, компания, департамент, город), вкладка «Согласование» (фрагмент
`bpm_ProcActionsFragment`), окно подписки (кнопка «Подписаться»). После сохранения — возврат в browse; при открытии/закрытии — синхронизация дочерних позиций.

Навигация внутри формы: постоянный sidebar (визуальный образ → название → label-навигация
разделов активной вкладки → контекст вакансии → предупреждение) и рабочая область
(toolbar → 12 вкладок TabSheet → прокручиваемый контент → постоянный footer с действиями).
Пункты label-навигации — визуальные указатели секций вкладки «Вакансия» (borderless-кнопки
без invoke: Java READ_ONLY, допустимо по §3.5 общего контракта).

### Краткий обзор бизнес-логики поведения (Behavior Summary)

- Открытие → загрузка `openPositionDc` (view `openPosition-edit-view`, без LOB) → `onAfterShow` догружает логотипы проекта/владельца и фиксирует baseline изменяемых полей. Новости, approval-процесс и все LOB (включая `comment`/`commentEn` и LOB-описания типа позиции) догружаются lazy при первом открытии соответствующей вкладки.
- Переключение вкладок → lazy-загрузка LOB/коллекций при первом открытии (10 флагов `*Loaded`).
- Изменение проекта/компании/типа позиции → каскадная подстановка (департаменты, город, описания, название вакансии).
- Сохранение → валидация вилки зарплаты и уникальности `vacansyID` → сбор подписчиков и уведомления (email/Telegram) → после коммита синхронизация дочерних позиций.
- Приоритет → авто-дата закрытия +7 дней; таймер 60 с обновляет обратный отсчёт и автозакрывает вакансию.
- Визуальный слой не запускает loaders, не меняет `*Loaded`-флаги, не создаёт новые бизнес-значения и не перехватывает lifecycle.

---

## 1. Точка вызова и контекст (Invocation & Context)

- `@UiController("hunttech_OpenPosition.edit")`, `@UiDescriptor("open-position-edit.xml")`, `@EditedEntityContainer("openPositionDc")`, `@LoadDataBeforeShow`.
- `dialogMode height="900px" width="1400px"` (модальное окно; изменена только ширина/высота по решению арбитра 9-1; openMode DIALOG и вызовы browse не менялись).
- Источники вызова: browse `OpenPositionBrowse` (create/edit), lookup-экраны, `JobCandidateEdit` (только просмотр связанной позиции).

## 2. Связь с моделью данных (Data & Entity Binding)

- Сущность: `OpenPosition`; контейнер `openPositionDc` (instance, view `openPosition-edit-view` — все поля вкладок кроме LOB + inline-свойства `laborAgreement` (`laborAgreement-openPosition-tab-view`) и `skillsList` (`skillTree-openPosition-tab-view`)).
- Коллекции вкладок: `laborAgreementDc` (LaborAgreement через join openPositions), `commentsOpenPositionDc` (OpenPositionComment, dateComment desc), `someFilesesDc` (SomeFilesOpenPosition),
`openPositionSkillsListsDc` (SkillTree, skillName), `procAttachmentsDc` (bpm$ProcAttachment, cacheable), `openPositionNewsDc` (OpenPositionNews + priorityNews, cacheable).
- Лента комментариев (вкладка «Комментарии») строится из двух источников: `commentsOpenPositionDc`
  (OpenPositionComment, view `openPositionComment-edit-view`) и feedback-итераций
  `hunttech_IteractionList` (JPQL `e.vacancy = :openPosition and e.iteractionType.signFeedback = true`,
  view `iteractionList-view`). В `iteractionList-view` для `recrutier` задекларированы
  `userAvatar`/`officialPhoto` (view `_minimal`) и `fileImageFace` (`_local`) — их читает
  `ExtUser.resolveProfilePhoto()` при отрисовке аватара автора; без них LAZY-доступ к `userAvatar`
  на detached-сущности давал ValidationException «null Session» (UNFETCHED ATTRIBUTE ACCESS)
  при первом открытии вкладки.
- Options-контейнеры: `openPositionParentDc` (родительские позиции, cacheable), `positionTypesDc` (Position без «(не использовать)», cacheable), `projectNamesDc` (Project не закрытые,
  cacheable), `companyNamesDc` (cacheable), `companyDepartamentsDc`, `citiesDc` (cacheable), `gradeDc` (cacheable).
- Защита от автозагрузки без параметров (`preventAutoLoadUntilParameterSet` в `onInit`): `laborAgreementDl`, `commentsOpenPositionDl`, `someFilesesDl`, `openPositionSkillsListsDl`,
  `procAttachmentsDl` (параметр вкладки), а также `openPositionNewsLc` (новости грузятся только при открытии вкладки «Новости»), `projectNamesLc` (только из `initProjectNameField`
  с `withOpenPositionCheckBox=true`), `companyDepartamentsLc` (только при выборе компании). Без защиты CUBA игнорировал бы не заданный параметр JPQL-условия и выбирал ВСЕ строки
  таблицы при каждом открытии формы.
- Facets: `timer closedVacancyTimer` (delay 60000, autostart=false, repeating) — автозакрытие по `closingDate`.
- Lazy-догрузка LOB: `loadPositionWithDescriptionLobs` (reload с view на LOB-поля) по вкладкам; `loadJobDescriptionTab` (вкладка «Описание должности») — `comment`/`commentEn` + LOB-описания типа позиции.
- Все `dataContainer`, `property`, `optionsContainer`, `required`, validators, actions и `invoke` сохранены без изменений (см. §7).

## 3. Иерархия и взаимосвязь форм (Form Hierarchy)

- Родитель: `OpenPositionBrowse` и lookup-экраны.
- Дочерние/диалоги: `SelectCitiesLocation` (выбор городов), `ProcActionsFragment` (BPM), справочные lookup-экраны picker-полей (Project, Company, City, Grade, Position).
- Фрагменты: `bpm_ProcActionsFragment` (вкладка «Согласование»).
- Связанные экраны: `JobCandidateEdit` использует узкий view `openPosition-edit-view` (строка 1117 views.xml).

## 4. Модель поведения и интерактивность (Behavior Model)

### 4.1 Lifecycle

- `onInit` → карты options + `preventAutoLoadUntilParameterSet` для 5 параметризованных коллекций вкладок; `onBeforeShow` (×2) → инициализация типа позиции, проекта, таймера, блокировок, иконок файлов, подписки-группы; LOB, новости и approval-процесс при открытии НЕ грузятся (перенесены на lazy-вкладки).
- `onAfterShow` → логотипы проекта/владельца (`initProjectImagesOnOpen`), `setTopLabel` (шапка: комиссии, статус), baseline изменяемых полей (`openPositionText`, `startLetterText`, зарплаты), `syncSidebarNavigation` (показ набора label-навигации вкладки по умолчанию «Вакансия» с активным первым пунктом).
- `onTabSheetOpenPositionSelectedTabChange` → `syncSidebarNavigation` (переключение набора label-навигации sidebar под активную вкладку) + lazy-загрузка вкладки при первом открытии (флаги `jobDescriptionLobsLoaded`, `exerciseLoaded`, `memoLoaded`, `templateLetterLoaded`, `skillsLoaded`,
  `filesLoaded`, `commentsTabLoaded`, `laborAgreementLoaded`, `newsTabLoaded`, `approvalLoaded`).
- Lazy-вкладка «Описание должности»: `loadJobDescriptionTab()` — `loadMainTabLobs()` (reload `comment`/`commentEn`), `ensurePositionLobsLoaded()` (reload `standartDescription`/`whoIsThisGuy` типа позиции), `initPositionTypeDescriptionFields()`, установка базы `openPositionText` (чтобы первое ручное изменение описания фиксировалось в новостях).
- Lazy-вкладка «Новости»: `loadOpenPositionNewsTab()` — `openPositionNewsLc.setParameter("openPosition", …)` + `load()`.
- Lazy-вкладка «Согласование»: `ensureApprovalProcessLoaded()` — `procAttachmentsDl.setParameter("entityId", …)` + `load()` + `procActionsFragment.initializer().standard().init(PROCESS_CODE, …)`; выполняется и для новых позиций (стартовое состояние фрагмента).
- `closedVacancyTimer` → тик каждые 60 с: обновление обратного отсчёта, автозакрытие.

### 4.2 Скрытые вычисления

- Каскады: проект → компания/департамент/город/описания/название; тип позиции → описания (RU/EN, «кто этот парень», стандартное) и навыки; приоритет → `setClosingWeek` (+7 дней); город →
название; смена грейда → префикс названия вакансии (`onGradeLookupPickerFieldValueChange`) — **только при действии пользователя** (`isUserOriginated=true`); событие привязки грейда при открытии формы игнорируется, чтобы не затирать сохранённое `vacansyName` строкой «<Грейд> null».
- Пересчёт комиссий: `calculateComission` (компания/ресурсер/рекрутер), `calculateResearcherSalary`/`calculateRecrutierSalary`, итоговые подписи
`setResearcherSalaryLabel`/`setRecrutierSalaryLabel`, HTML-шапка `setTopLabel`.
- Звёздный рейтинг sidebar: `refreshSidebarRating()` — среднее арифметическое `rating` всех
  взаимодействий `hunttech_IteractionList` по текущей вакансии (`avg(e.rating + 1)` по
  `e.vacancy.id`, как в JobCandidateEdit), округление `Math.round`, классы
  `open-position-rating-red-1…blue-5` / `-empty`; presentation-only, вызывается из
  `refreshSidebarInfoCard()` (onBeforeShow + listener `openPositionDc`).
- Генераторы: `openPostionNewsDataGridDetailsGenerator` (детали новости), `skillImageColumnRenderer`, `openPositionSkillsListTableIsCommentColumnGenerator`.
- Лента комментариев: `loadCommentsTab` (при первом открытии вкладки, флаг `commentsTabLoaded`) →
  `setCommentsOpenPositionScroll` (OpenPositionComment) + `setCommentOpenPositionScrollIteractionList`
  (feedback-итерации, JPQL по `iteractionType.signFeedback = true`); каждая запись — через
  `getCommentBox(...)`: аватар автора (`setUserProfilePhoto` → `ExtUser.resolveProfilePhoto()`),
  подпись «кандидат / должность» (null-safe: при отсутствии `personPosition` у кандидата должность
  подставляется пустой строкой), рейтинг-звёзды, дата, кнопка «Ответить» (`replyButtonInvoke` → `createComment`).
- Сканирование описания: `addShortDescription` (короткое описание) и `rescanJobDescription`/`reloadSkillsForOpenPositionTab` (пересборка навыков).

### 4.3 Валидация/сохранение

- `openPositionFieldSalaryMinValidator`/`MaxValidator` — вилка мин ≤ макс.
- `onBeforeCommitChanges` → `checkDuplicatePositionId` (дубль `vacansyID` → диалог), `publishEventMessage`, сбор подписчиков (`getAllSubscibers`, `getSubscriberMaillist`,
`getRecrutiersMaillist`), `sendOpenPositionMessage`/`sendClosePositionMessage`.
- `onBeforeCommitChanges1/3/4` → синхронизация коллекций (навыки, соглашения, комментарии) и статусов.
- `onAfterCommitChanges` → `notifyTelegramOpenPositionChange`; `onAfterCommitChanges1` → `openCloseChildVacancy` (дочерние позиции).
- Ограничения: `vacansyNameField` required, `positionTypeField`/`remoteWorkField`/`numberPositionField`/`priorityField` required; LOB-редакторы блокируются до полной загрузки
(`screenFullyLoaded`).

## 5. Логика управляющих элементов (Actions & Buttons Logic)

- `generateVacancyNameFieldButton` → `generateNameFieldButton` → `generateVacancyName` (название по типу/проекту/городу).
- `gradeLookupPickerField` (смена грейда пользователем) → `onGradeLookupPickerFieldValueChange` — замена или добавление префикса грейда в `vacansyNameField`; событие привязки значения при открытии формы (`isUserOriginated=false`) игнорируется — название остаётся как сохранено в сущности (без «<Грейд> null»).
- `addCity` → `addListCity` — окно `SelectCitiesLocation` (массовый выбор городов по стране).
- `setSalaryFieldButton` → `setSalaryFieldButtonInvoke` — подстановка зарплаты из рейтов аутстафа.
- `scanJDButton` → `addShortDescription` — извлечение короткого описания; `rescanSkills` → `rescanJobDescription` — пересборка навыков.
- `addOpenPositionNewsButton` → `addOpenPositionNewsButton` — создание новости.
- `subscribePositionButton` → `subscribePosition` — подписка рекрутёра.
- `windowCommitAndCloseButton` → `windowCommitAndClose` (сохранить и закрыть); `windowCloseButton` → закрыть без сохранения.
- Чекбоксы вкладок: `needExerciseCheckBox`, `needMemoCheckBox`, `needLetterCheckBox` — показывают/блокируют редакторы; `openClosePositionCheckBox` (скрыт) → `disableEnableFields`;
`signDraftCheckBox` → черновик.
- Таблицы: `laborAgreementDataGrid` (create/edit/remove, inline-редактирование), `someFilesTable` (add/create/edit/remove), `openPostionNewsDataGrid` (create/remove),
`openPositionSkillsListTable` (только просмотр, actions закомментированы).
- Пункты label-навигации sidebar — borderless-кнопки **с рабочими клик-обработчиками**: в каждом наборе свой перечень пунктов (18 кнопок в 11 наборах по вкладкам `tabSheetOpenPosition`); каждый клик подсвечивает пункт (`label-nav-item-active`) и переводит фокус в первый элемент ввода соответствующей секции (штатная прокрутка ScrollBox). Наборы: «Вакансия» (5: Наименование, Параметры вакансии, Вакансия, Команда, Зарплатное предложение), «Трудовой договор» (2: Трудовой договор, Детали оплаты — секции «Оплата ресерчерам/рекрутерам» без полей ввода не включены), «Описание должности» (3: Опыт работы, Описание должности, Краткое описание), «Файлы», «Тестовое задание», «Памятка кандидату», «Шаблон письма», «Требуемые Навыки», «Новости» (по 1), «Согласование» и «Комментарии» (по 1, без перевода фокуса — блоки без полей ввода: BPM-фрагмент и лента label-элементов). **Label-навигация показывается только на вкладках с двумя и более блоками ввода** («Вакансия», «Трудовой договор», «Описание должности»): `syncSidebarNavigation` скрывает весь контейнер `openPositionEditorNavigation` (заголовок + наборы) на одноблочных вкладках («Файлы», «Тестовое задание», «Памятка кандидату», «Шаблон письма», «Требуемые Навыки», «Новости», «Согласование», «Комментарии»); их наборы остаются в XML/Java только как контракт (@Named/@Subscribe) и никогда не показываются.

## 6. Визуальная компоновка элементов (Visual Layout Schema)

### 6.1. Новая двухпанельная структура (редизайн 2026-08-05)

```text
layout (edit-screen-layout open-position-editor, dialogMode 1400×900)
├── vbox openPositionSidebar (edit-sidebar, 270px / 250px ≤1366px — из shared)
│   ├── vbox openPositionSidebarVisual (edit-sidebar-visual)
│   │   ├── hbox openPositionEditorLogoBox (open-position-editor-logo-box, height AUTO) — изображения рядом по центру
│   │   │   ├── projectLogoImage (96×96) + projectOwnerImage (96×96, оба oval 96, MIDDLE_CENTER)
│   │   └── vbox openPositionSidebarIdentity (edit-sidebar-identity)
│   │       ├── labelOpenPosition (edit-sidebar-title + open-position-editor-title-clamp; значение — название должности positionType.getInstanceName(), по центру)
│   │       └── signDraftLabel (edit-sidebar-subtitle)
│   │   └── vbox openPositionRatingBox (open-position-editor-status) — блок «Средний рейтинг вакансии» (эталон JobCandidateEdit statusRatingRow): полоса с нижним разделителем rgba(255,255,255,.16), подпись + звёзды; компоновка вертикальная (капшн сверху, звёзды строкой ниже), как у остальных блоков sidebar; подпись и звёзды центрированы по горизонтали (text-align: center контейнера)
│   │       ├── label openPositionRatingCaptionLabel (open-position-editor-sidebar-caption, width 100%, msgAvgRating «Средний рейтинг вакансии») — капшн 10.5px/700 uppercase rgba(248,250,252,0.62)
│   │       └── label openPositionRatingLabel (h3) — значение пустое, звёзды рисует CSS :before по классам open-position-rating-red-1…blue-5 (1–5 ★, 19px/700 #ffb11b) / open-position-rating-empty (☆☆☆☆☆); среднее арифметическое rating взаимодействий IteractionList по текущей вакансии считает Java refreshSidebarRating() — avg(e.rating + 1) по e.vacancy.id, округление Math.round (та же методика, что в JobCandidateEdit)
│   ├── hbox vacancyStateSummary (open-position-editor-vacancy-state-summary + edit-sidebar-summary) — блок срочности (эталон IteractionListEdit): пара ячеек 50/50 с рамками выделения (фон rgba(255,255,255,.05), border rgba(255,255,255,.12), radius 6px, padding 8×9)
│   │   ├── vbox vacancyStatusSummary (open-position-editor-vacancy-state-cell + open-position-editor-sidebar-info-row, width 50%) — ячейка «Статус вакансии»
│   │   │   ├── label «Статус вакансии:» (open-position-editor-sidebar-caption, msgStatusOfVacansy)
│   │   │   └── hbox vacancyStatusValueBox (open-position-editor-sidebar-value-row, expand statusOfVacansyLabel)
│   │   │       └── label statusOfVacansyLabel (open-position-editor-sidebar-value + open-position-editor-status-value) — ЧЕРНОВИК (h3-gray) / ОТКРЫТА (h3-green) / ЗАКРЫТА (h3-red), значение и цвет — Java refreshSidebarStatus()
│   │   ├── vbox vacancyPrioritySummary (open-position-editor-vacancy-state-cell + open-position-editor-sidebar-info-row, width 50%) — ячейка «Приоритет»
│   │   │   ├── label «Приоритет» (open-position-editor-sidebar-caption, msgPriority)
│   │   │   └── hbox vacancyPriorityValueBox (open-position-editor-sidebar-value-row, expand currentPriorityLabel)
│   │   │       ├── image trafficLighterImage (open-position-editor-priority-image) — светофор StandartPriorityVacancy
│   │   │       └── label currentPriorityLabel (open-position-editor-sidebar-value) — Draft/Paused/Low/Normal/High/Critical
│   │   ├── button openClosePositionButton (small + open-position-editor-open-close-button) — «Закрыть вакансию» (открыта: OPEN_CLOSE false/none) / «Открыть вакансию» (закрыта: OPEN_CLOSE true), надпись — Java `refreshOpenCloseButton()`; invoke `openClosePositionToggle` → toggle OPEN_CLOSE + синхронизация чекбокса «Закрыта» (блокировка полей `disableEnableFields`) + обновление sidebar; нотификация всем пользователям `UiNotificationEvent` при сохранении (`onBeforeCommitChanges3 → publishEventMessage`); визуал 1:1 с footer-кнопками JobCandidateEdit («Создать взаимодействие» — `job-candidate-profile-footer .v-button`: 100%, min-height 38px, 14/600, фон rgba(255,255,255,.06), border rgba(255,255,255,.34), radius 5px, hover фон .10)
│   │   ├── label «Работа:» (open-position-editor-sidebar-caption, msgRemoteWorkSidebar)
│   │   └── label remoteWorkSidebarLabel (open-position-editor-sidebar-value) — Офис/Удаленная работа/Гибрид (50/50)
│   ├── vbox openPositionEditorNavigation (label-navigation; виден только на вкладках с 2+ блоками ввода — «Вакансия», «Трудовой договор», «Описание должности»; на одноблочных вкладках скрыт целиком Java `syncSidebarNavigation`)
│   │   ├── openPositionEditorNavActiveSectionsLabel (label-nav-title + open-position-editor-section-title «Разделы активной вкладки» — стиль заголовка = caption карточки «Информация»: #ffb11b 15px/700, полоса rgba(255,255,255,.045), border-bottom rgba(255,255,255,.14))
│   │   └── 5 пунктов borderless label-nav-item: «Наименование» (`openPositionEditorNavName`, active по умолчанию на вкладке «Вакансия»), «Параметры вакансии» (`openPositionEditorNavParams`), «Вакансия» (`openPositionEditorNavVacancy`), «Команда» (`openPositionEditorNavTeam`), «Зарплатное предложение» (`openPositionEditorNavSalary`); устаревшие пункты со старым id «Параметры вакансии» (`openPositionEditorNavProject`) и «Количество персонала» (`openPositionEditorNavPersonnel`) удалены; все 5 пунктов — рабочие label-навигации с кликом: «Наименование» (фокус в ID), «Параметры вакансии» (фокус в приоритет), «Вакансия» (фокус в должность `positionTypeField`), «Команда» (фокус в radio «Команда/Вакансия»), «Зарплатное предложение» (фокус в мин. зарплату)
│   ├── groupBox openPositionEditorInfoCard («Информация», edit-accordion-section + open-position-editor-info-card;
│   │   фон карточки, caption и content — transparent, сквозь неё виден фон sidebar .edit-sidebar (#172638 градиент);
│   │   caption-стиль (полоса #ffb11b 15px/700 с border-bottom) — эталон заголовков «Разделы активной вкладки»
│   │   и «Контекст вакансии» через класс open-position-editor-section-title)
│   │   └── vbox openPositionEditorInfoCardVBox — 6 HTML-label: infoPositionLabel («Должность» → vacansyName),
│   │       infoProjectLabel («Проект» → projectName), infoOwnerLabel («Владелец проекта» → projectOwner),
│   │       infoCityLabel («Город» → cityPosition), infoSalaryMaxTcLabel («Зарплата МАХ (ТК)» → salaryMax),
│   │       infoSalaryMaxIeLabel («Зарплата МАХ (ИП)» → salaryIE) — значения формирует refreshSidebarInfoCard()
│   ├── vbox openPositionEditorSummary (edit-sidebar-summary + open-position-editor-summary-grid)
│   │   ├── openPositionEditorContextLabel (label-nav-title + open-position-editor-section-title «Контекст вакансии» — стиль заголовка = caption карточки «Информация»)
│   │   ├── citiesLabel (open-position-editor-summary-caption) — сводка городов (Java changeCityListsLabel)
│   │   ├── contextGradeLabel («Грейд» → grade.gradeName), contextRegistrationLabel («Оформление» → registrationForWork), contextStatusLabel («Статус» → signDraft/openClose) — open-position-editor-summary-value, значения формирует refreshSidebarInfoCard()
│   │   ├── labelTopComissionRecrutier · labelTopComissionResearcher (итоговые комиссии, HTML, setTopLabel)
│   ├── closedVacancyInfoLabel (edit-sidebar-warning)
│   └── vbox openPositionSidebarSpacer (edit-sidebar-spacer)
└── vbox openPositionWorkspace (edit-workspace)
    ├── hbox openPositionToolbar (edit-toolbar)
    │   └── vbox openPositionToolbarTitleBox
    │       ├── openPositionEditorToolbarTitle (edit-toolbar-title «Редактирование открытой позиции»)
    │       └── openPositionEditorToolbarDescription (edit-toolbar-description)
    ├── tabSheet tabSheetOpenPosition (framed edit-tabs open-position-editor-tabs) — 12 вкладок
    └── vbox forExpand (edit-footer-actions open-position-editor-footer)
        ├── hbox statusHBox (open-position-editor-owner-row) → ownerTextField (borderless)
        └── hbox editActions (open-position-editor-footer-actions, flex-wrap: wrap)
            ├── subscribePositionButton (open-position-editor-secondary-action)
            └── hbox commitActions (open-position-editor-commit-actions) — группа завершения
                ├── windowCommitAndCloseButton (open-position-editor-primary-action)
                └── windowCloseButton (open-position-editor-secondary-action)
```

### 6.2. Вкладка «Вакансия» (tabOpenPosition)

Поток карточек в scrollBox `mainTabScrollBox` (вертикальный поток, все секции — на всю ширину контента, `open-position-editor-cards-row` → `flex-direction: column`):

- **Блок «Наименование»** (`openPositionNameGroupBox`, самый верх вкладки) — одна строка `openPositionNameRow` (`row-title`, без expand): внутренний ID `vacansyIDTextField` + грейд `gradeLookupPickerField` (options `gradeDc`) + название вакансии `vacansyNameField` (required) + кнопка `generateVacancyNameFieldButton` «Генерировать» (`invoke="generateNameFieldButton"` сохранён); ID и кнопка «Генерировать» — фиксированная ширина **110px**, Грейд — **143px** (110 + 30%), Вакансия растягивается на свободное место (`width=100%` + `box.expandRatio=1`); поля перенесены сюда из карточки «Вакансия» (строка `vacancyNameHBox` удалена, ID и Грейд убраны из `vacancyTopRow`);
- **Блок «Параметры вакансии»** (`openPositionParamsGroupBox`, caption `msgVacancyParams`, ниже «Наименования», до ряда карточек) — 2 строки: **строка 1** `vacancyTopRow` (`row-half`): приоритет `priorityField` (required, `width=AUTO`, класс `open-position-editor-priority-field`) + удалёнка `remoteWorkField` (`width=AUTO`, required) + дата закрытия `closingDateDateField` — компактные поля по содержимому в одну линию; **строка 2** `priorityFieldsHBox`: комментарий к приоритету `commentPriority` (caption «Комментарий», на всю ширину). Поля перенесены сюда из карточки «Вакансия» (2026-08-07, требование пользователя);
- **Блок «Вакансия»** (`openPositionEditorIdentifiersCard`, ниже) — плоская карточка, все строки напрямую (обёртки `vacancyInputGroupBox`, `commandFieldHBox`, `projectTypeGroupBox`, `closingDateFieldsHBox`, `vacancyParamsPositionRow`, `vacancyParamsProjectRow`, `projectFieldsVBox` удалены; приоритет/удалёнка/дата закрытия/комментарий к приоритету перенесены в «Параметры вакансии»), строки сверху вниз:
  - **Строка 1** `vacancyMainRow` (`row-half`): должность `positionTypeField` (ratio 3, required, options `positionTypesDc`) + проект `projectNameField` (ratio 4, required, options `projectNamesDc`) — свободное место делится 3:4;
  - **Строка 2** `vacancyCompanyRow` (`row-half`): компания `companyNameField` + департамент `companyDepartamentField` (оба required, options из соответствующих Dc) — в одну линию, половина/половина;
  - **Строка 3** `vacancyCityRow` (`row-half`) → `cityFieldsHBox` (expand на город): город `cityOpenPositionField` (required, options `citiesDc`) + кнопка `addCity` («Добавить», `invoke` сохранён) — отдельной строкой под компанией/департаментом;
  - **Строка 4** `remoteCommentRow`: комментарий к формату удалённой работы `remoteWorkCommentField` (на всю ширину, expand);
  - **Строка 5** `vacancyFlagsRow`: все три чекбокса скрыты (`visible="false"`): `signDraftCheckBox` «Черновик вакансии», `internalProjectCheckBox` «Эксклюзивная вакансия» (Java-управление видимостью отключено — `setInternalProject()` больше не вызывает `setVisible`) и служебный `openPositionCloseCheckBox`... `openClosePositionCheckBox` (Java работает только со значением);
  - **Строка 6** `projectFilterCheckBoxesHBox`: фильтры списка проектов `onlyOpenProjectCheckBox`/`withOpenPositionCheckBox` — **скрыты** (`visible="false"`), Java-подписки и логика фильтрации `projectNamesLc` сохранены (значение по умолчанию `withOpenPosition=true` из `initProjectNameField()`);
- **Блок «Команда / Вакансия»** (`commandOrVacancyGroupBox`, ниже) — 3 строки: (1) `commandOrPositionRow` — radio `commandOrPosition` (тип «команда/вакансия», опции из Java `setOptionsMap` «Команда»→1/«Вакансия»→0; подпись `msgCommandOrPosition` убрана по требованию, `width=100%`); (2) `commandPersonnelRow` (`open-position-editor-row-half`) — `parentOpenPositionField` «Команда» (родительская позиция) + `numberPositionField` «Количество персонала/команд» (caption переключается в Java по типу записи), половина/половина через `box.expandRatio=1`; (3) `more10NumberPositionRow` — `more10NumberPositionField` «Более 10 открытых вакансий» отдельной строкой.
- **Блок «Зарплатное предложение»** (`salaryGroupBox`, caption `msgSalaryProposal`; бывший «Заработная плата») — 4 строки сверху вниз: **строка 1** `salaryMinMaxHBox` — «Зарплата мин» `openPositionFieldSalaryMin` + «Зарплата мах» `openPositionFieldSalaryMax` в линию; **строка 2** `salaryIEHBox` — «Зарплата ИП» `openPositionFieldSalaryIE`; **строка 3** `salaryCheckBoxesRow` (`open-position-editor-checkboxes-row`) — чекбоксы «Ориентируемся на запрос кандидата» `salaryCandidateRequestCheckBox` + «Фиксированный лимит зарплаты» `salaryStrongLimitCheckBox` в линию; **строка 4** `salaryCommentRow` — «Комментарий» `salaryCommentTextFiels` на всю ширину.

Каждая секция: `edit-accordion-section` + `showAsPanel="true"`, `collapsable/collapsed` сохранены. Поля — `edit-form-control`; строки — `open-position-editor-field-row` с вариантами
(`row-title`, `row-grade`, `row-position`, `row-half`, `row-salary`, `row-wide`).

**Sidebar-навигация** (`openPositionEditorNavigation`): активный пункт синхронизирован с открытой вкладкой (вердикт арбитра `00-arbitration-sidebar-active.md`): на вкладке «Вакансия» активен `openPositionEditorNavName` («Наименование» — первый пункт набора), на остальных вкладках активный пункт снимается; базовый `label-nav-item` не изменяется; управление — `addStyleName/removeStyleName("label-nav-item-active")` в `onTabSheetOpenPositionSelectedTabChange` (переключение вкладок) и в `onAfterShow` (инициализация при открытии: вкладка по умолчанию «Вакансия» → пункт «Наименование» активен сразу). **Все 5 пунктов — рабочие label-навигации** (клик снимает подсветку со всех пунктов, подсвечивает выбранный и переводит фокус в первое поле секции — штатная прокрутка ScrollBox; паттерн `selectSection` из эталона IteractionListEdit): «Наименование» (`openPositionEditorNavName` → `vacansyIDTextField`, caption `mainMsg://msgName`), «Параметры вакансии» (`openPositionEditorNavParams` → `priorityField`, caption `msg://msgVacancyParams`; добавлен 2026-08-07), «Вакансия» (`openPositionEditorNavVacancy` → `positionTypeField` — приоритет переехал в «Параметры вакансии», caption `mainMsg://openPositionEditorNavVacancy`), «Команда» (`openPositionEditorNavTeam` → `commandOrPosition`, caption `msg://msgCommand`) и «Зарплатное предложение» (`openPositionEditorNavSalary` → `openPositionFieldSalaryMin`, caption `msg://msgSalaryProposal`). Устаревшие пункты «Параметры вакансии» со старым id (`openPositionEditorNavProject`) и «Количество персонала» (`openPositionEditorNavPersonnel`) удалены из XML и Java. Кнопки навигации — flex-контейнеры (`display: flex; align-items: center` в shared `edit-screen-shared-styles.scss`): текст пункта центрируется внутри подсветки (раньше из-за `display: block` у Vaadin-кнопки текст выпадал ниже подсветки).

**Иконки опций полей** (`optionIconProvider`/`optionStyleProvider`): «Приоритет вакансии»
(`priorityField`), «Удалёнка» (`remoteWorkField`), «Регистрация для работы»
(`registrationForWorkField`) показывают иконку выбранной опции слева от текста.
Локальное правило `.open-position-editor .edit-form-control.v-filterselect > .v-icon`
(20px, по центру слева) + `padding-left: 40px` у `input` — иконка не наезжает на текст
(shared-селектор `.edit-form-control .v-filterselect` не матчится, т.к. stylename стоит
на самом `.v-filterselect`). `priorityField` дополнительно получил локальный класс
`open-position-editor-priority-field` (`flex: 0 0 150px`) — узкая строка «Настройки
вакансии» не сжимает поле, текст опции читается на всех viewport.

### 6.3. Вкладка «Трудовой договор» (laborAgreementTab)

`laborAgreementGroupBox` (edit-accordion-section): параметры оформления `outstaffParamsHBox` (`registrationForWorkField`, `outstaffingCostTextField`, `setSalaryFieldButton`) и
`laborAgreementDataGrid` (open-position-editor-table-variant5) с buttonsPanel. После таблицы — платёжные секции, перенесённые из скрытой вкладки `tabPayments` (решение арбитра 9-2), в порядке:
`groupBoxPaymentsDetail` (детали оплаты, collapsed=true сохранён) → `groupBoxPaymentsResearcher` → `groupBoxPaymentsRecrutier`. ID, bindings и visibility-контракты сохранены; вкладка
`tabPayments` остаётся скрытой технической вкладкой (инвариант `@Named("tabSheetOpenPosition.tabPayments")`).

### 6.4. Остальные вкладки

- **Описание должности**: `workExperienceGroupBox` (collapsed=true), аккордеон `openPositionAccordion` (4 RichTextArea → `edit-form-control` + `open-position-editor-richtext-variant5`),
ряд `shortDescriptionHBox` (`shortDescriptionTextArea` + `scanJDButton`).
- **Файлы / Навыки / Новости**: таблицы `someFilesTable`, `openPositionSkillsListTable` (treeDataGrid), `openPostionNewsDataGrid` → `open-position-editor-table-variant5`.
- **Тестовое / Памятка / Шаблон письма**: checkBox + RichTextArea (`edit-form-control` + richtext-variant5).
- **Согласование**: `procActionsBox` → `edit-accordion-section` + `open-position-editor-group-tab` (резерв 44px под caption).
- **Комментарии**: `commentsScrollBox` → `open-position-editor-comments-scroll`.

### 6.5. Стили

- Общие классы (shared `edit-screen-shared-styles.scss`, не изменялся): `edit-*` и `label-*`; ширина sidebar 270px/250px — из shared.
- Локальный namespace `.open-position-editor` (7 идентичных theme-local partial `open-position-editor.scss`): тёмная sidebar `#172638 → #132130 → #0f1b28`, label-навигация по эталону
`IteractionListEdit` (hover белый на rgba(255,255,255,.08), active `#ffb11b` на rgba(255,177,27,.12) + жёлтая border-left), карточки, responsive-строки, таблицы/редакторы variant5,
footer, primary/secondary actions. Локальные классы: `open-position-editor` (root), `-logo-box`, `-title-clamp`, `-tabs`, `-field-row` (+row-варианты), `-cards-row`, `-subsection`,
`-primary-section`, `-group-tab`, `-summary-grid`, `-summary-caption/value`, `-payment-section`, `-payments-columns`, `-labor-tab-content`, `-labor-params`, `-table-variant5`,
`-richtext-variant5`, `-comments-scroll`, `-footer`, `-footer-actions`, `-owner-row`, `-primary-action`, `-secondary-action`, `-spacer`, `-tab-content`, `-table-view`, `-project-section`,
`-commit-actions` (группа OK/Отмена в footer), `-checkboxes-row` (строка признаков «Черновик»/«Эксклюзивная»),
`-richtext-section`, `-row-remote`, `-priority-field` (фикс. ширина 150px для поля приоритета с иконкой),
`-section-title` (заголовки секций sidebar «Разделы активной вкладки» и «Контекст вакансии» — стиль caption «Информация»: две inset-линии полосы — белая сверху `rgba(255,255,255,1) 0 1px 0 0 inset`, светлая снизу `rgba(244,244,244,1) 0 -1px 0 0 inset` — + border-bottom).
- Порядок слоёв в каждой теме: `theme base → edit-screen-shared-styles → open-position-editor`.
- Шрифт заголовков вкладок `tabSheetOpenPosition` — 1:1 с эталоном JobCandidateEdit (`.job-candidate-tabs`): caption 14px/600, цвет `#26384c`, line-height 48px, padding 0 10px; hover `#1264b5`; selected — `$v-selection-color` + border-bottom 3px. Правило `.open-position-editor-tabs .v-tabsheet-tabitem .v-caption` во всех 7 темах (CDP-контроль 2026-08-08, тема halo: невыбранные вкладки rgb(38,56,76) = #26384c, 14px/600).

### 6.6. Последовательность заполнения

1. **Основные параметры**: ID и название вакансии (генерация), грейд → настройки: дата закрытия, приоритет, черновик → команда/вакансия и родительская позиция.
2. **Проект и локация**: тип позиции и формат удалённой работы → проект и департамент → компания и город (+массовое добавление городов).
3. **Количество персонала и заработная плата**: число позиций («более 10»), вилка зарплаты мин/макс, зарплата ИП, комментарий и лимит.
4. **Оформление (вкладка «Трудовой договор»)**: регистрация для работы, стоимость аутстафа, соглашения, затем схемы оплаты компании/ресерчерам/рекрутерам.
5. **Тексты и материалы**: описания RU/EN, стандартное описание, «кто это», короткое описание, тестовое задание, памятка, шаблон письма, навыки, новости, комментарии.

### 6.7. Responsive-контракт

| Viewport | Sidebar | Workspace | Поведение |
|---|---|---|---|
| >1366px | 270px (shared) | content max-width 1480px, центрирован | полный вид |
| ≤1366px | 250px (shared media) | сжатие | пары карточек/полей переходят в одну колонку через flex-wrap |
| любые | вертикальный scroll внутри, горизонтальный запрещён | без горизонтального скролла (кроме tab bar и таблиц); заголовки вкладок TabSheet — одна строка, при нехватке ширины горизонтальный скролл полосы (стандартный Vaadin TabSheet: `overflow-x: auto`, `flex-wrap: nowrap`, `flex-shrink: 0` на вкладках) | `min-width: 0; max-width: 100%; box-sizing: border-box` |

Адаптация выполняется flex-контейнерами и `flex-wrap` без вложенных `@media` (ограничение Sass CUBA 7.3).

## 7. Контракт компонентов (Component ID | Тип | Binding | Визуальный раздел | UNCHANGED)

Полная карта 120 функциональных компонентов с source/target и allowed_visual_change зафиксирована в
`.team/OpenPositionEdit/02-component-map.csv` (разделитель «;»). Ниже — ключевые реперные компоненты:

| Component ID | Тип | Binding | Визуальный раздел | UNCHANGED |
|---|---|---|---|---|
| `labelOpenPosition` | label | — (значение: Java `setTopLabel` → название должности `positionType.getInstanceName()`) | sidebar identity (title, по центру) | id, value-контракт Java |
| `signDraftLabel` | label | — (значение: Java) | sidebar identity (subtitle) | id, value-контракт Java |
| `projectLogoImage` / `projectOwnerImage` | ovaFallbackImage | — | sidebar visual (96×96 / 96×96, oval 96, MIDDLE_CENTER, рядом в `openPositionEditorLogoBox`) | id, fallback, Java-инъекции |
| `citiesLabel` | label | — (значение: Java) | sidebar summary | id, value-контракт Java |
| `statusOfVacansyLabel` | label | — (значение: Java `refreshSidebarStatus` → signDraft/openClose; цвет h3-gray/h3-green/h3-red) | sidebar пара 50/50 (`vacancyStatusSummary`, ячейка «Статус вакансии») | id, value-контракт Java |
| `trafficLighterImage` / `currentPriorityLabel` | image / label | — (значение: Java `refreshSidebarPriority` → StandartPriorityVacancy) | sidebar пара 50/50 (`vacancyPrioritySummary`, ячейка «Приоритет») | id, Java-инъекции, value-контракт Java |
| `openClosePositionButton` | button | — (caption: Java `refreshOpenCloseButton` → msgCloseVacancy/msgOpenVacancy по OPEN_CLOSE; invoke `openClosePositionToggle`) | sidebar, под парой «Статус вакансии»+«Приоритет» (до строки «Работа:») | id, caption, invoke, stylename (small + open-position-editor-open-close-button), Java-инъекция |
| `contextGradeLabel` / `contextRegistrationLabel` / `contextStatusLabel` | label | — (значение: Java `refreshSidebarInfoCard` → `grade.gradeName` / `registrationForWork` / `signDraft`+`openClose`) | sidebar summary («Грейд» / «Оформление» / «Статус») | id, htmlEnabled, value-контракт Java |
| `labelTopComissionRecrutier` / `labelTopComissionResearcher` | label | — (значение: Java) | sidebar summary | id, htmlEnabled, value-контракт Java |
| `closedVacancyInfoLabel` | label | — (значение: Java-таймер) | sidebar warning | id, icon, value-контракт Java |
| `vacansyIDTextField` / `gradeLookupPickerField` / `vacansyNameField` | textField / lookupPickerField | `vacansyID` / `grade` / `vacansyName` | блок «Наименование» (`openPositionNameGroupBox`, строка `openPositionNameRow`: ID/кнопка — 110px, Грейд — 143px, Вакансия expandRatio=1) | id, dataContainer, property, required, options |
| `priorityField` / `remoteWorkField` / `closingDateDateField` / `commentPriority` | lookupField / lookupField / dateField / textField | `priority` / `remoteWork` / `closingDate` / `priorityComment` | блок «Параметры вакансии» (`openPositionParamsGroupBox`, caption `msgVacancyParams`: `vacancyTopRow` + `priorityFieldsHBox`; перенесены из карточки «Вакансия» 2026-08-07) | id, dataContainer, property, required |
| `commandFieldHBox` (удалён из XML) | groupBox | — | обёртка «Настройки вакансии» упразднена, элементы вынесены в карточку `openPositionEditorIdentifiersCard` (строки 2–3) | — |
| `commandOrPosition` / `parentOpenPositionField` | radioButtonGroup / lookupPickerField | `commandCandidate` / `parentOpenPosition` | карточка «Команда / Вакансия» | id, dataContainer, property, required, options |
| `positionTypeField` | lookupPickerField | `positionType` | карточка «Вакансия» `openPositionEditorIdentifiersCard` (`vacancyMainRow`, focusComponent) | id, dataContainer, property, options, required |
| `projectNameField` / `companyDepartamentField` / `companyNameField` / `cityOpenPositionField` | lookupPickerField | `projectName` / — / — / `cityPosition` | карточка «Вакансия» `openPositionEditorIdentifiersCard` (`vacancyTopRow` / `vacancyParamsCompanyRow` / `vacancyParamsCityRow`) | id, options, property, required, actions |
| `numberPositionField` / `openPositionFieldSalaryMin` / `openPositionFieldSalaryMax` | textField | `numberPosition` / `salaryMin` / `salaryMax` | блок «Команда / Вакансия» (`personnelCountGroupBox`) / карточка «Зарплатное предложение» | id, dataContainer, property, validators |
| `laborAgreementDataGrid` | dataGrid | `laborAgreementDc` | laborAgreementTab (table-variant5) | id, dataContainer, actions, columns, editorEnabled |
| `groupBoxPaymentsDetail` / `groupBoxPaymentsResearcher` / `groupBoxPaymentsRecrutier` | groupBox | поля внутри (bindings сохранены) | laborAgreementTab (после таблицы) | id, captions, collapsed, Java-видимость |
| `tabPayments` | tab | — | скрытая техническая вкладка | id, visible=false, @Named-путь |
| `openPositionAccordion` + 4 tabs | accordion | LOB-поля richTextArea | tabJobDescription | id, @Named-пути, required/editable |
| `someFilesTable` / `openPostionNewsDataGrid` / `openPositionSkillsListTable` | table / dataGrid / treeDataGrid | collection containers | вкладки Файлы/Новости/Навыки | id, actions, columns, renderers |
| `ownerTextField` | textField | `owner` | footer (owner-row) | id, dataContainer, editable=false, enable=false |
| `subscribePositionButton` / `windowCommitAndCloseButton` / `windowCloseButton` | button | actions/invoke | footer-actions | id, invoke/action, порядок |
| `tabSheetOpenPosition` | tabSheet | — | workspace (edit-tabs) | id, 12 вкладок, lazy-загрузка Java |
| `closedVacancyTimer` | timer (facet) | — | facets | id, delay, autostart=false, repeating |

## 8. UI-референсы

- `docs/ui/images/OpenPositionEdit/01_open_position_tab_main_halo_1920x1080.png` — утверждённый проектный рендер (halo, 1920×1080): двухпанельная компоновка, toolbar «Редактирование открытой
позиции», 12 вкладок, sidebar с «РАЗДЕЛЫ АКТИВНОЙ ВКЛАДКИ» (6 пунктов) и «КОНТЕКСТ ВАКАНСИИ». Рендер отражает целевой визуальный язык; точное воспроизведение отдельных бизнес-элементов
рендера ограничено решениями арбитра (см. §9): вкладка «Оплата и контакты» остаётся скрытой, статус «• позиция открыта» не дублируется, sidebar 270/250px по общему контракту.
- Эталон визуального языка: `docs/ui/OpenPositionEditPreview_Spec.md` (preview-форма, паттерны variant5, group-tab, footer, field-row).

## 9. Что не изменялось

- Java: `OpenPositionEdit.java` — изменён ТОЛЬКО по вердикту арбитра `00-arbitration-sidebar-active.md` (пункт A): добавлены 6 `@Named`-инъекций nav-кнопок и визуальная синхронизация `label-nav-item-active` в существующем обработчике `onTabSheetOpenPositionSelectedTabChange`. Бизнес-логика, lazy-загрузка, вычисления, сохранение — без изменений.
- Entity, enum, справочники, сервисы, loaders, JPQL, `views.xml`, DataContext, Liquibase, БД.
- Другие формы, включая `open-position-edit-preview.xml` / `OpenPositionEditPreview.java` и browse.
- Shared SCSS `edit-screen-shared-styles.scss` (7 копий) и SCSS других экранов.
- Component ID, `dataContainer`, `property`, `optionsContainer`, actions, существующие `invoke` (в т.ч. `generateNameFieldButton`), `required`, `visible`, `enabled`, `editable`, `readonly`, validators, captions (msg-ключи).
- `visible="false"` у `tabPayments`, `openClosePositionCheckBox`, `internalProjectCheckBox`, `commanExperienceRadioButton`, `lastOpenVacancyDateField`.
- Закомментированные actions вкладки «Навыки» не восстанавливались.

## 10. Функциональные границы (корректирующий этап «Проект», 2026-08-05)

```text
component IDs: UNCHANGED
bindings: UNCHANGED
actions: UNCHANGED
handlers: UNCHANGED
entity: UNCHANGED
reference data: UNCHANGED
loaders: UNCHANGED
JPQL: UNCHANGED
views: UNCHANGED
other screens: UNCHANGED
```

Исключение (CONDITIONALLY_ALLOWED, вердикт арбитра `00-arbitration-sidebar-active.md`): `OpenPositionEdit.java`
+27 строк — 6 `@Named`-инъекций nav-кнопок и визуальная синхронизация `label-nav-item-active` в существующем
обработчике `onTabSheetOpenPositionSelectedTabChange` (lazy-загрузка и бизнес-логика не тронуты).
Полное подтверждение — `06-cuba-verification.md` (FUNCTIONAL_CONTRACT: UNCHANGED, STATUS: VERIFIED).

## 11. Runtime-проверка (корректирующий этап «Проект»)

- **HEAD**: `2ff1f129ec1378c043293a8d7ba30f77316e0988` (local deploy, Tomcat, http://localhost:8080/hrm/ → HTTP 200)
- **Темы**: halo (light), hunttech-modern-dark (dark) — обе PASS
- **Viewport**: 1920×1080, 1440×900, 1366×768, 1280×800 — PASS (без горизонтальной прокрутки, bodyHscroll=0)
- **Проверенные состояния**: sidebar active «Идентификаторы» на вкладке «Проект»; снят на «Трудовой договор»;
  восстановлен при возврате; ID+Вакансия в строке; Грейд+«Генерировать» в строке (кнопка компактная w=121px);
  карточки ID/«Команда / Вакансия» и «Количество персонала»/«Заработная плата» вертикально (w=1354px)
- **Скриншоты**: `.ai/reports/open-position-edit-project-tab/2ff1f129ec1378c043293a8d7ba30f77316e0988/screenshots/`
  (`light-{1920,1440,1366,1280}-project-tab.png`, `dark-{1920,1366}-project-tab.png`,
  `sidebar-project-active.png`, `sidebar-neighbor-active.png`)
- **Независимая инспекция**: `05-visual-review.md` — VISUAL_CONTRACT: PASS (12/12 пунктов)
- **CUBA-верификация**: `06-cuba-verification.md` — FUNCTIONAL_CONTRACT: UNCHANGED, STATUS: VERIFIED
- **Результат**: замечания предыдущего browser-раунда (непокрытие DOM-обёртки `v-expand` селекторами
  cards-row/row-grade/row-title) найдены и исправлены; повторная полная проверка — PASS.
  Функциональные замечания: нет.

## История изменений

| Дата | Изменение |
|------|-----------|
| 2026-08-11 | Sidebar: блок **«Средний рейтинг вакансии»** (`openPositionRatingBox`, stylename `open-position-editor-status`) — звёздный рейтинг 1:1 с блоком «Рейтинг» в sidebar JobCandidateEdit (`statusRatingRow`): подпись `openPositionRatingCaptionLabel` (msgAvgRating, капшн `open-position-editor-sidebar-caption` 10.5px/700 uppercase, width 100%) + `openPositionRatingLabel` (h3, пустой — звёзды рисует CSS `:before`). Вертикальная компоновка (капшн сверху, звёзды строкой ниже) — длинная подпись «Средний рейтинг вакансии» не влезает в одну строку со звёздами в sidebar (наезд v-expand при expandRatio 1/2, исправлено после CDP-проверки). Подпись и звёзды центрированы по горизонтали относительно sidebar (text-align: center контейнера, требование пользователя). Методика (как в JobCandidateEdit `setRatingLabel`/`loadAverageRating`): среднее арифметическое `rating` всех взаимодействий `hunttech_IteractionList` по текущей вакансии — JPQL `avg(e.rating + 1)` по `e.vacancy.id`, округление `Math.round`; Java `refreshSidebarRating()` (вызов из `refreshSidebarInfoCard`, presentation-only). SCSS `.open-position-editor-status` + классы `open-position-rating-red-1…blue-5`/`-empty` (19px/700 #ffb11b, line-height 30px, content ★/☆) во всех 7 темах (md5=1); блок расположен под Images (`openPositionEditorLogoBox`) и identity, до пары «Статус+Приоритет». Preview-XML: скрытая заглушка `openPositionRatingLabel` (контракт @Inject). Контрактные тесты `sidebarRatingBlockMirrorsJobCandidateEdit` + `sidebarRatingScssMirrorsJobCandidateStatusInAllThemes`; business-логика и view не менялись |
| 2026-08-11 | Фикс `IllegalStateException: Cannot get unfetched attribute [laborAgreement]` при сохранении вакансии с открытой вкладкой «Трудовой договор» (регрессия 2026-08-10, замена `dataContext.merge` на прямой сеттер в sync-методах). Коллекции `laborAgreement`/`skillsList` задекларированы inline-свойствами в view контейнера `openPositionDc` (open-position-edit.xml и open-position-edit-preview.xml); woven-сеттер коллекции читает getter для change detection и падает, если атрибут не во fetch group контейнера. ensure-методы reload+setter удалены. Общий `openPosition-edit-view` (nested в job-candidate-edit.xml) не менялся. Тесты: DetachedObject 6/6 (новый контракт inline-декларации), ScreenViewIntegrity 8/8, LayoutContract 20/20, OptionsIntegrity 5/5 PASS |
| 2026-08-10 | Производительность открытия формы: убран главный N+1 — cacheable-загрузчики опций `companyNamesLc`/`positionTypesLc`/`citiesDl` переведены в обычные (entity cache делал точечный find() по ID на каждую строку списка: ~5 658 SELECT Company на открытие, 7-9 с). SQL при открытии 6 215 → 216; время 9-14 с → 2.7-3.1 с (p50). Поведение/внешний вид не менялись; добавлен `OpenPositionEditOptionsIntegrityTest` (view опций + регрессия cacheable) |
| 2026-08-10 | Багфикс toggle «Закрыть/Открыть вакансию» (sidebar `openClosePositionButton`) + оптимизация коммита (побочный эффект аудита производительности). (1) `onBeforeCommitChanges` → `syncLaborAgreementToEntity`/`syncSkillsListToEntity`: вместо `dataContext.merge(reloaded)` — только перенос коллекции (`setLaborAgreement`/`setSkillsList`). `merge` копировал состояние из БД поверх пользовательских изменений (open_close не попадал в UPDATE — вакансия не закрывалась; лишние ~40 SELECT SYS_FILE и version-only UPDATE на каждый коммит). (2) `onBeforeShow1`: чекбокс `openClosePositionCheckBox` при null-значении синхронизируется из фактического `openClose` сущности — прежний жёсткий `false` затирал закрытую вакансию (при повторном входе кнопка показывала «Закрыть вакансию» вместо «Открыть вакансию», статус «ОТКРЫТА»). (3) `onBeforeCommitChanges1`: та же синхронизация вместо жёсткого `false` (закрытая вакансия молча открывалась при любом сохранении). (4) из `onAfterShow` убран `rescanJobDescription()` — парсинг навыков выполняется только при фактическом изменении описания (открытие формы −2-3 SQL, поведение/дизайн не менялись; навыки вкладки по-прежнему lazy, пересборка при правке текста через ValueChange). CDP-проверка: toggle→OK→`open_close=t`; повторный вход в закрытую → «Открыть вакансию»/«ЗАКРЫТА»; сохранение закрытой без изменений → осталась закрытой; toggle «Открыть»→OK→`open_close=f`. XML, views, дизайн, бизнес-логика не менялись |
| 2026-08-09 | Строка «Наименование» (`openPositionNameRow`): поле «Грейд» `gradeLookupPickerField` расширено на 30% — **110px → 143px** (требование пользователя; ID `vacansyIDTextField` и кнопка «Генерировать» `generateVacancyNameFieldButton` остались 110px, не тронуты). Правки: XML (width="143px" + комментарий) и SCSS `.open-position-editor-row-title` во всех 7 темах — слот 3 (Грейд) вынесен из группы «width: 110px !important» в отдельный блок «width: 143px !important», слоты 1/7 (ID/«Генерировать») не изменены (именно это !important-правило перебивало XML-ширину). Деплой: buildScssThemes + копирование build/web/VAADIN/themes → webapps/hrm/VAADIN/themes + рестарт (без копирования тем браузер получает старый styles.css — слот остаётся 110px при инлайн-143px). CDP-замер (тема halo, форма «Создать»): grade 143px, id 110px, generate 110px. Контракт `OpenPositionEditLayoutContractTest.nameSection...` обновлён: Грейд — width="143px"; ScreenViewIntegrityTest PASS; 7 SCSS идентичны (sha256=1) |
| 2026-08-09 | Оптимизация открытия формы: в onInit добавлены preventAutoLoadUntilParameterSet для трёх loaders, параметр которых не установлен на момент @LoadDataBeforeShow — иначе CUBA игнорирует JPQL-условие и грузит **все** строки таблицы: openPositionNewsLc (все новости всех позиций — EXPLAIN Seq Scan 35 649 строк / ~40ms), projectNamesLc (все проекты с глубоким view, дублировался вторым load() из initProjectNameField), companyDepartamentsLc (все департаменты всех компаний, не cacheable — при каждом открытии). Данные догружаются прежним кодом: новости — loadOpenPositionNewsTab (вкладка «Новости»), проекты — initProjectNameField (withOpenPositionCheckBox=true), департаменты — onCompanyNameFieldValueChange (выбор компании). Поведение формы не изменено; compileJava + ScreenViewIntegrityTest + контрактные тесты OpenPositionEdit PASS; деплой/restart PASS; CDP-проверка всех вкладок без UNFETCHED/DETACHED ошибок |
| 2026-08-09 | Lazy-load оптимизация скорости открытия формы: (1) LOB-описания comment/commentEn и LOB-описания типа позиции (standartDescription/whoIsThisGuy) убраны из onBeforeShow — догружаются lazy при первом открытии вкладки «Описание должности» (loadJobDescriptionTab, флаг jobDescriptionLobsLoaded; openPositionText обновляется после догрузки, чтобы уведомления об изменении описания работали); (2) новости позиции — из onBeforeShow в lazy loadOpenPositionNewsTab (вкладка «Новости», флаг newsTabLoaded); (3) BPM-инициализация вкладки «Согласование» — из onBeforeShow1 в lazy ensureApprovalProcessLoaded (флаг approvalLoaded, для новых позиций тоже); (4) companyNamesLc → cacheable=true (справочник компаний больше не грузится SQL при каждом открытии). Итог: при открытии формы не выполняются синхронные reload 4 LOB, запрос новостей, загрузка BPM-вложений и инициализация ProcActionsFragment. compileJava + ScreenViewIntegrityTest + OpenPositionEditDetachedObjectTest PASS |
| 2026-08-09 | Sidebar: под парой «Статус вакансии»+«Приоритет» добавлена кнопка `openClosePositionButton` — toggle OPEN_CLOSE: открыта (false/none) → «Закрыть вакансию» (`msgCloseVacancy`), закрыта (true) → «Открыть вакансию» (`msgOpenVacancy`), надпись — Java `refreshOpenCloseButton()`; invoke `openClosePositionToggle` (инверсия OPEN_CLOSE + синхронизация чекбокса «Закрыта» `openClosePositionCheckBox` → блокировка полей `disableEnableFields` + обновление sidebar); нотификация всем пользователям — стандартный CUBA-инструмент `UiNotificationEvent` (GlobalEvents addon) через существующий `onBeforeCommitChanges3 → publishEventMessage` при сохранении; визуал 1:1 с footer-кнопками JobCandidateEdit (`job-candidate-profile-footer .v-button`): 100%, min-height 38px, 14/600, фон rgba(255,255,255,.06), border rgba(255,255,255,.34), radius 5px, hover .10 — класс `open-position-editor-open-close-button` во всех 7 темах (md5=1); messages_ru: +`msgCloseVacancy`/`msgOpenVacancy`; preview-XML: скрытая заглушка кнопки; контрактные тесты `vacancyCloseButtonSitsUnderStatusPriorityPair` + `vacancyCloseButtonScssCarriesJobCandidateFooterLookInAllThemes` |
| 2026-08-09 | Sidebar: блок срочности приведён к эталону IteractionListEdit — вместо вертикального `vacancyPrioritySummary` (приоритет + удалёнка) создана **горизонтальная пара ячеек 50/50** `vacancyStateSummary`: левая ячейка `vacancyStatusSummary` («Статус вакансии», caption `msgStatusOfVacansy` + `statusOfVacansyLabel` — «ЧЕРНОВИК»/«ОТКРЫТА»/«ЗАКРЫТА» с цветовыми классами h3-gray/h3-green/h3-red, Java `refreshSidebarStatus()`), правая ячейка `vacancyPrioritySummary` («Приоритет», светофор `trafficLighterImage` + `currentPriorityLabel`); ячейки — `open-position-editor-vacancy-state-cell` с рамками выделения 1:1 с `iteraction-list-vacancy-state-cell` (фон rgba(255,255,255,.05), border rgba(255,255,255,.12), radius 6px, padding 8×9); подписи/значения — новые классы `open-position-editor-sidebar-caption`/`-value`/`-value-row` (шрифты 1:1 с `iteraction-list-sidebar-caption`/`-value`: caption 10.5px/700 uppercase, value 13px/500); формат работы (`remoteWorkCaptionLabel`/`remoteWorkSidebarLabel`) — отдельная строка под парой; удалены неиспользуемые SCSS-классы `open-position-editor-priority-summary/-value-row/-value`; SCSS во всех 7 темах идентичны (md5=1); Java: +`@Inject statusOfVacansyLabel`, +`refreshSidebarStatus()` (вызов из `refreshSidebarInfoCard`); preview-XML: скрытая заглушка `statusOfVacansyLabel`; контрактные тесты `vacancyStatePairMirrorsIteractionListEdit` + `vacancyStatePairScssCarriesHighlightFramesInAllThemes` |
| 2026-08-09 | Label-навигация sidebar показывается только на вкладках с **двумя и более блоками ввода**: `syncSidebarNavigation()` скрывает целиком контейнер `openPositionEditorNavigation` (заголовок «Разделы активной вкладки» + наборы пунктов) на одноблочных вкладках — «Файлы», «Тестовое задание», «Памятка кандидату», «Шаблон письма», «Требуемые Навыки», «Новости», «Согласование», «Комментарии»; наборы этих вкладок больше не переключаются (всегда `setVisible(false)`) и ветви их активации удалены из `syncSidebarNavigation`, сами наборы/кнопки сохранены в XML и Java как контракт (@Named/@Subscribe). На много-блочных вкладках («Вакансия» 5, «Трудовой договор» 2, «Описание должности» 3) поведение прежнее: показывается набор активной вкладки, подсвечивается первый пункт. Введена константа `TABS_WITH_SIDEBAR_NAVIGATION`; новое `@Inject`-поле `openPositionEditorNavigation` (+ скрытая заглушка в preview-XML для инжекции витрины); XML-комментарии наборов актуализированы; контрактный тест `sidebarNavigationSetsFollowTabs` дополнен проверкой контейнера и правил видимости |
| 2026-08-09 | Вкладки TabSheet OpenPositionEdit возвращены к **стандартной реализации Vaadin TabSheet — одна строка + горизонтальный скролл**: убраны `flex-wrap: wrap` / `white-space: normal` / `width: 100%` с `.open-position-editor-tabs .v-tabsheet-tabs` (заменены на `nowrap` + `width: auto`), `overflow: visible` на tabcontainer заменён на `overflow-x: auto; overflow-y: hidden`, вкладкам добавлен `flex-shrink: 0` — 12 вкладок более не переносятся на вторую строку (tabcontainer сжался со 145px до 49px), при нехватке ширины полоса прокручивается горизонтально. CDP-контроль (тема halo): scrollWidth 1931 > clientWidth 858 (1440px) и > 318 (900px), scrollLeft устанавливается, `document.documentElement.scrollWidth == innerWidth` (страница не выходит за экран). SCSS во всех 7 темах идентичны (md5=1); buildScssThemes + deploy/restart PASS; Java и XML не менялись |
| 2026-08-09 | Исправлен UNFETCHED ATTRIBUTE ACCESS при открытии вкладки «Комментарии»: `iteractionList-view` (views.xml) дополнен для `recrutier` атрибутами `userAvatar`/`officialPhoto` (view `_minimal`) — `ExtUser.resolveProfilePhoto()` читал LAZY-связь `userAvatar` на detached-рекрутере и падал с ValidationException «null Session», из-за чего лента feedback-итераций не отрисовывалась. Дополнительно подпись «кандидат / должность» в `getCommentBox(IteractionList)` стала null-safe (при NULL `personPosition` у кандидата должность — пустая строка вместо NPE). Верифицировано CDP (тема halo): лента отображает и OpenPositionComment, и feedback-комментарии (Мешков, Ребров, Холуев, Пряников) без ошибок; `compileJava`/`deploy`/`restart` PASS, в catalina.out нет ValidationException/IllegalStateException |
| 2026-08-09 | Label-навигация sidebar стала **попланочной**: единый набор пунктов заменён на 11 наборов по вкладкам `tabSheetOpenPosition` (контейнеры `openPosition*TabNavigation` внутри `openPositionEditorNavigation`); `syncSidebarNavigation()` показывает набор активной вкладки и подсвечивает первый пункт, клики подсвечивают пункт и фокусируют первый элемент ввода секции. Состав: «Вакансия» 5 пунктов (как было), «Трудовой договор» 2 (соглашения → `registrationForWorkField`, детали оплаты → `radioButtonGroupPaymentsType`), «Описание должности» 3 (опыт → `workExperienceRadioButton`, описание → `openPositionRichTextArea`, короткое → `shortDescriptionTextArea`), «Файлы» → `someFilesTable`, «Тестовое задание» → `needExerciseCheckBox`, «Памятка кандидату» → `needMemoCheckBox`, «Шаблон письма» → `needLetterCheckBox`, «Требуемые Навыки» → `openPositionSkillsListTable`, «Новости» → `openPostionNewsDataGrid`, «Согласование» и «Комментарии» — только подсветка (блоки без полей ввода). Новые keys `openPositionEditorNavPaymentsDetail/WorkExperience/ShortDescription/TemplateLetter/Comments`; секции «Оплата ресерчерам/рекрутерам» (label-only) в навигацию не включены. Java: поля наборов/кнопок, `activateNavigationItem`, `resetNavigationActiveStyles` по всем наборам; компиляция PASS; XML-комментарии актуализированы |
| 2026-08-08 | Шрифт заголовков вкладок TabSheet OpenPositionEdit приведён 1:1 к эталону JobCandidateEdit (`.job-candidate-tabs`): `font-size` 12px → **14px**, цвет `mix($v-font-color,$v-panel-background-color,86%)` → **#26384c**, hover `$v-selection-color` → **#1264b5** (font-weight 600, line-height 48px, padding 0 10px совпадали — сохранены) в `.open-position-editor-tabs .v-tabsheet-tabitem .v-caption` во всех 7 темах (файлы идентичны, md5=1); `buildScssThemes` + deploy/restart PASS; CDP-контроль (тема halo): невыбранные вкладки rgb(38,56,76) = #26384c 14px/600, выбранная — `$v-selection-color` (halo #4d7ab2) + border-bottom 3px; контрактный тест дополнен `tabsCaptionsMatchJobCandidateEditFont`; Java и бизнес-логика не менялись |
| 2026-08-08 | Блок «Контекст вакансии» (`openPositionEditorSummary`) дополнен тремя контекстными строками (presentation-only; значения формирует `refreshSidebarInfoCard()` из edited-объекта — тот же паттерн, что у карточки «Информация»): **«Грейд»** (`contextGradeLabel` → `grade.gradeName`), **«Оформление»** (`contextRegistrationLabel` → `registrationForWork` через `registrationForWorkName()` — аутстаффинг/рекрутинг/все варианты по `StandartRegistrationForWork` + screen-messages `mainmsg*`), **«Статус»** (`contextStatusLabel` → `signDraft` → «Черновик», иначе `openClose` → «Открыта»/«Закрыта» по `msgOpenBy`/`msgOpecnClosePosition`); строки вставлены между `citiesLabel` и комиссиями, стиль `open-position-editor-summary-value` (SCSS не менялся, 7 тем идентичны); в preview-XML добавлены скрытые копии (контракт `@Inject` preview: `previewProvidesAllInheritedUiComponentsAndListenerTargets`); контрактный тест `sidebarVacancyContextContainsGradeRegistrationStatus` (id, htmlEnabled, stylename, порядок строк); Java: +3 `@Inject`, +2 helper; business-логика и view не менялись |
| 2026-08-08 | Заголовки секций sidebar «Разделы активной вкладки» (`openPositionEditorNavActiveSectionsLabel`) и «Контекст вакансии» (`openPositionEditorContextLabel`) приведены к стилю заголовка карточки «Информация»: добавлен класс `open-position-editor-section-title` (stylename `label-nav-title open-position-editor-section-title` в XML), SCSS-правило `.edit-sidebar .open-position-editor-section-title` во всех 7 темах повторяет caption инфокарточки 1:1 — `#ffb11b` 15px/700, line-height 21px, min-height 36px, padding 7px 11px, фон-полоса rgba(255,255,255,.045), border-bottom rgba(255,255,255,.14), letter-spacing normal; в `@media (max-height:800px)` добавлен override `.open-position-editor .edit-sidebar .open-position-editor-section-title` (padding не ужимается, как у label-nav-title). CDP-контроль (тема halo): computed-стили трёх заголовков идентичны 1:1 (color rgb(255,177,27), bg rgba(255,255,255,.043), 15px/700, min-height 36px, padding 7px 11px, border-bottom 1px rgba(255,255,255,.14)). Java и бизнес-логика не менялись; buildScssThemes + deploy/restart PASS; 7 тем идентичны |
| 2026-08-08 | Footer OpenPositionEdit переведён на **одну строку** как в IteractionListEdit (CDP-контроль, 1440×812 и 1366×768): кнопки «Подписаться на вакансию»/«ОК»/«Отмена» вылезали за нижний край экрана (footer bottom=818 > 812, двухстрочная компоновка). Причины: (1) `forExpand` (vbox) держал две строки — `statusHBox` (владелец позиции) и `editActions` (кнопки); (2) `flex-wrap: wrap` на actions переносил «Подписаться» на отдельную строку; (3) контент вкладки переполнял workspace (762px > 747px), выталкивая footer вниз. Фикс (SCSS, XML не менялся): `.open-position-editor-footer` — `position:absolute` к низу workspace (прижат всегда, как эталон), `flex-direction:row` (владелец слева, кнопки справа), padding 5px 14px, min-height 50px; `.open-position-editor-owner-row` — `flex:0 1 auto`, `width:auto`, `white-space:nowrap`; `.open-position-editor-footer-actions` — `flex:1 1 auto`, `flex-wrap:nowrap`, `min-width:0` на слотах. Результат CDP: 1440×812 — кнопки y=758..798 (запас 14px, footer bottom=803 в границах workspace); 1366×768 — кнопки y=714..754 (запас 14px, footer bottom=759 < 768). Все 7 тем идентичны (md5=1) |
| 2026-08-08 | Sidebar приведён к эталону IteractionListEdit (CDP-контроль): (1) **title под изображениями = название должности** — `setTopLabel()` теперь пишет в `labelOpenPosition` значение `positionTypeField.getValue().getInstanceName()` (NamePattern «Ru / En» сущности Position) вместо «наименование вакансии (компания : проект)»; обновление добавлено в оба обработчика `onPositionTypeFieldValueChange` (первый и `ValueChange1`); CSS title/subtitle — `text-align: center` (+`justify-content:center` на flex-title) во всех 7 темах; (2) sidebar 270→**312px**, padding 14/16/12, caption «Информация» **#ffb11b 15px/700**; (3) nav-блок display:block, padding 10/0/4, border-top rgba .16, заголовок nav 11px/700 rgba(248,250,252,.62) **без uppercase** (`text-transform:none`); (4) секции рабочей области 46px/68% без border-left, footer-кнопки 14px/600; (5) фикс наезда в блоке «Приоритет»: значение в hbox (`currentPriorityLabel`) переведено на `inline-block` + `vertical-align:middle` (ранее display:block выпадал на baseline и перекрывал подпись «Работа:»); (6) блок «Информация» без рамки (`border-color:transparent; border-width:0`); (7) компактизация для влезания: v-spacing sidebar 6→4px через `.edit-sidebar .v-expand > .v-spacing` (ранее селектор `.edit-sidebar > .v-spacing` не матчил — spacing лежит внутри Vaadin-обёртки `v-expand`), хвостовой spacing скрыт; CDP: warning bottom 793 < sidebar bottom 803, scrollH 758 (clientH 747); XML/Java бизнес-логики не менялись (только UI-значение title); 7 тем идентичны (md5=1) |
| 2026-08-07 | Sidebar: изображения `projectLogoImage` и `projectOwnerImage` приведены к эталону IteractionListEdit — оба 96×96 (oval 96), `align="MIDDLE_CENTER"`, рядом друг с другом в `openPositionEditorLogoBox` (hbox `height="AUTO"`, `spacing="true"`, `align="MIDDLE_CENTER"`); аватар больше не смещён в угол (убран `BOTTOM_RIGHT`); CSS `.open-position-editor-logo-box` во всех 7 темах: изображения 96px (было 88px), hbox `height: auto` (было 96px); Java-инъекции и `setValueSource(null)`/`setSource(FileDescriptorResource)` не менялись; контрактный тест дополнен `sidebarImagesMatchIteractionListEditSizeAndAlignment` |
| 2026-08-07 | Sidebar label-навигация вкладки «Вакансия» сокращена до 4 рабочих пунктов: добавлен «Наименование» (`openPositionEditorNavName`, caption `mainMsg://msgName`, фокус в `vacansyIDTextField`), «Вакансия» перенацелена на начало карточки (фокус `priorityField` вместо ID, переехавшего в блок «Наименование»), «Команда / Вакансия» переименована в «Команда» (`openPositionEditorNavTeam`, caption `msg://msgCommand`, добавлен клик-обработчик `onOpenPositionEditorNavTeamClick` с фокусом в radio `commandOrPosition`), «Зарплатное предложение» сохранено; удалены пункты «Параметры вакансии» (`openPositionEditorNavProject`) и «Количество персонала» (`openPositionEditorNavPersonnel`) из XML и Java (`@Named`, `resetNavigationActiveStyles`, обработчики); активный по умолчанию пункт — «Наименование» (onAfterShow + `onTabSheetOpenPositionSelectedTabChange`); контрактный тест дополнен `sidebarNavigationListsNameVacancyTeamSalaryInOrder`; preview не затронут (скрытые служебные кнопки остались); Java не изменялся вне навигации |
| 2026-08-07 | Новый блок «Параметры вакансии» (`openPositionParamsGroupBox`, caption `msgVacancyParams`) после блока «Наименование»: в него перенесены из карточки «Вакансия» (`openPositionEditorIdentifiersCard`) строка `vacancyTopRow` (приоритет `priorityField`, удалёнка `remoteWorkField`, дата закрытия `closingDateDateField`) и строка `priorityFieldsHBox` (комментарий `commentPriority`); чекбоксы фильтров проектов `onlyOpenProjectCheckBox` («Только открытые проекты») и `withOpenPositionCheckBox` («Только с открытыми вакансиями») скрыты (`visible="false"`), Java-подписки и логика `setProjectClosedFilter()`/`initProjectNameField()` сохранены; sidebar label-навигация расширена до 5 пунктов — добавлен «Параметры вакансии» (`openPositionEditorNavParams` → фокус `priorityField`, caption `msgVacancyParams`) между «Наименованием» и «Вакансией»; фокус пункта «Вакансия» переведён с `priorityField` на `positionTypeField` (должность — первое поле карточки после переноса); business-id, bindings, options, actions, required, invoke не менялись; Java-изменения: инжекция `openPositionEditorNavParams`, `resetNavigationActiveStyles()` + NavParams, обработчик клика, фокус NavVacancy → `positionTypeField` |
| 2026-08-07 | Строка «Наименование» (`openPositionNameRow`): ID `vacansyIDTextField`, грейд `gradeLookupPickerField` и кнопка `generateVacancyNameFieldButton` «Генерировать» получили фиксированную одинаковую ширину **110px** (ровно как поле ID, `box.expandRatio` у всех трёх убран), Вакансия `vacansyNameField` растягивается (`box.expandRatio=1` вместо 7); CSS `.open-position-editor-row-title` переписан: слоты 1/3/7 — `flex: 0 0 auto` + `width:110px !important`, слот 5 (Вакансия) — `flex: 1 1 0` + `width:100%` (селекторы учитывают v-spacing между слотами — позиции 1,3,5,7 — в обеих формах DOM: слоты прямо в строке и внутри `.v-expand`); контрактный тест переименован в `nameSectionHoldsSingleRowWithFixedWidths` (проверяет 110px у трёх и expandRatio=1 у Вакансии); business-id, bindings, options, actions, required, invoke не менялись; Java не затрагивался |
| 2026-08-07 | Блок «Наименование»: грейд `gradeLookupPickerField` перемещён в строку `openPositionNameRow` между ID и названием вакансии; пропорции строки переведены на относительные `box.expandRatio` **1:1:7:1** (ID, Грейд, «Генерировать» — по 1/10, Вакансия — 7/10; у всех `width=100%`, `expand` с hbox снят); CSS `.open-position-editor-row-title` переписан под 4 слота с flex 1:1:7:1 (без абсолютных px) во всех 7 темах (sha256 идентичны); контрактный тест переименован в `nameSectionHoldsSingleRowWithRelativeRatios` и проверяет expandRatio; business-id, bindings, options, actions, required, invoke не менялись; Java не затрагивался |
| 2026-08-07 | Блок «Наименование» в самом верху вкладки «Вакансия» (`openPositionNameGroupBox`, caption `mainMsg://msgName`, `edit-accordion-section`): одна строка `openPositionNameRow` (`row-title`, `expand="vacansyNameField"`) — ID `vacansyIDTextField` (`width=AUTO`) + название вакансии `vacansyNameField` (`width=100%`, растягивается) + кнопка `generateVacancyNameFieldButton` «Генерировать» (`width=AUTO`); поля перенесены из карточки «Вакансия» (ID убран из `vacancyTopRow`, строка `vacancyNameHBox` удалена); CSS `.open-position-editor-row-title` переписан под 3 слота во всех 7 темах (ID `flex:0 0 auto`, expand `flex:5 1 160px`, кнопка `flex:0 0 auto`), темы приведены к идентичности (sha256 один); контрактный тест дополнен `nameSectionHoldsSingleRowWithAutoAndExpandFields`; business-id, bindings, options, actions, required, invoke не менялись; Java не затрагивался |
| 2026-08-07 | Выравнивание sidebar по эталону IteractionListEdit (CDP-контроль, 5 итераций): (1) nav-пункты 34px/6px 10px/17px → **27px/3px 10px/20px** (эталон), nav-заголовок 4px 10px 5px → **6px 10px 8px**; (2) устранено вертикальное переполнение sidebar: visual-блок ужат (padding 6px 12px 4px, spacing 4px, identity 3px 2px 0), поля инфокарточки lh 18px + v-spacing 4px, caption/content карточки компактные, summary 3px 10px 2px, warning 3px 8px/14px — на 1440×812 весь контент влезает (warning bottom 797 < 803), на 1366×768 включён `@media (max-height:800px)` (пункты 25px, карточка 16px-поля) — warning 748 < 768; (3) Java (`OpenPositionEdit.onAfterShow`): инициализация активного пункта «Вакансия» при открытии (ранее active появлялся только после переключения вкладки) — визуальная синхронизация, бизнес-логика не тронута; (4) тонкий скроллбар sidebar (webkit, 6px, thumb rgba(255,255,255,.18)); CDP: активный пункт — bg rgba(255,177,27,.12), текст/маркер #ffb11b; hover пунктов — белый на rgba(255,255,255,.08) |
| 2026-08-06 | Адаптивность формы под разные разрешения экрана (CDP-проверка 1440/1366/1152/900/800): (1) вкладки TabSheet переносятся на новые строки (`flex-wrap` + `width:100%` на `.v-tabsheet-tabs`) — все 12 вкладок доступны, полоса не выходит за правый край; (2) контент вкладки сжимается (`min-width:0` на контент-цепочке, `height:auto` вместо `calc(100% - 49px)`); (3) кнопки footer компактнее (padding 14px, min-width 110px) — на разрешениях ≥1152 в одну строку, на узких переносятся, но footer прижат к низу окна и кнопки всегда видимы; `commitActions` — `nowrap` (ОК/Отмена всегда вместе) |
| 2026-08-06 | Кнопки footer выровнены в правом нижнем углу (CDP-замер в Halo: «Подписаться на вакансию»/«ОК»/«Отмена» на одной линии y=725, высота 40px). Причины рассинхрона: (1) у `commitActions` был `align="BOTTOM_RIGHT"` — ОК/Отмена прижимались к низу слота (y=727/h=38 против y=718/h=40 у «Подписаться»); (2) селектор высоты `.open-position-editor-footer-actions > .v-slot > .v-button` не матчил кнопки внутри вложенного `commitActions`. Фикс: убран align у `commitActions` в XML; селектор расширен до `.open-position-editor-footer-actions .v-button` во всех 7 темах |
| 2026-08-06 | Интервал «лейбл → поле» приведён к эталону IteractionListEdit (CDP-замер в Halo: высота лейбла 48→18px, зазор 35.4→4.9px). Root cause: общее правило `.edit-tabs .v-caption { min-height: 48px }` (edit-screen-shared-styles.scss) матчило подписи полей внутри `.edit-tabs` — селектор сужен до `.edit-tabs .v-tabsheet-tabitem .v-caption`; ранее также сужен `.open-position-editor-tabs .v-caption` → `.v-tabsheet-tabitem .v-caption`; убраны `margin-bottom: 5px` и `line-height: 17.55px` из `.open-position-editor-field-row .v-caption`. Все 7 тем |
| 2026-08-06 | Стили подписей полей `.open-position-editor-field-row .v-caption` приведены к эталону IteractionListEdit: убраны `margin-bottom: 5px` и явный `line-height: 17.55px` (дефолт Halo 1.55) — лейбл прижат к элементу ввода, во всех 7 темах |
| 2026-08-06 | Блок «Команда / Вакансия»: radio `commandOrPosition` — подпись убрана, вынесено на строку 1; «Команда» + «Количество персонала» — строка 2 (половина/половина); «Более 10 открытых вакансий» — строка 3 |
| 2026-08-06 | «Количество персонала» вынесено в блок «Команда / Вакансия` плоско: удалена вложенная секция `personnelCountGroupBox` (collapsable/`open-position-editor-subsection`), строка `numberPositionHBox` (`numberPositionField` + `more10NumberPositionField`) размещена напрямую в `commandOrVacancyGroupBox` после ячейки родительской позиции; sidebar-комментарий пункта «Количество персонала» актуализирован; preview не затронут (уже плоский); bindings/required/@Subscribe `more10NumberPositionField` сохранены; деплой не выполнялся |
| 2026-08-06 | Из label-навигации sidebar удалён пункт «Настройки вакансии» (`openPositionEditorNavSettings`): кнопка удалена из `open-position-edit.xml` и preview (служебная); из `OpenPositionEdit.java` удалены `@Named`-поле и `removeStyleName` в `resetNavigationActiveStyles` (обработчика клика не было — presentation-only); `compileJava` BUILD SUCCESSFUL; деплой не выполнялся |
| 2026-08-06 | Из label-навигации sidebar удалён пункт «Идентификаторы» (`openPositionEditorNavIdentifiers`): кнопка удалена из `open-position-edit.xml` и preview (служебная); из `OpenPositionEdit.java` удалены `@Named`-поле и `removeStyleName` в `resetNavigationActiveStyles`; на вкладке «Вакансия» активным стал первый оставшийся пункт «Вакансия» (`openPositionEditorNavVacancy`) — `label-nav-item-active` по умолчанию; `compileJava` BUILD SUCCESSFUL; деплой не выполнялся |
| 2026-08-06 | Sidebar label-навигация приведена к эталону IteractionListEdit 1:1 (контракт `HRM_HuntTech_Edit_Screen_Shared_Style_Contract.md` §3.1) во всех 7 темах (`open-position-editor.scss`): `.label-nav-item` — `min-height: 27px`, `padding: 3px 10px`, `line-height: 20px` (font 13px/600; значения уточнены 2026-08-07 по фактическому браузерному эталону IteractionListEdit — контракт §3.1 давал 34px, браузер показывает 27px); `.label-nav-title` — `padding: 6px 10px 8px` + явный `font-weight: 700` (11px/16px); проверен реальный `styles.css` hover-темы на сервере (новые значения присутствуют, специфичность локальных правил выше shared — каскад гарантирован); `buildScssThemes` + deploy/restart PASS; браузерная сверка требует входа в UI (креды admin/admin неверны — блокер для CDP-шага) |
| 2026-08-06 | Актуализирована sidebar label-навигация под текущую компоновку: обновлены XML-комментарии пунктов «Вакансия», «Настройки вакансии» (подраздел карточки: дата/приоритет/признаки), «Параметры вакансии» (строки параметров карточки: должность/проект/компания/департамент/город); Java-комментарии обработчиков `onOpenPositionEditorNavVacancyClick`/`onOpenPositionEditorNavProjectClick` приведены в соответствие (фокус в ID / в должность — как и раньше); поведение пунктов и 7 кнопок не менялось; preview не затронут (служебные скрытые кнопки) |
| 2026-08-06 | Фикс CSS `.open-position-editor-row-title` (темы hover, hunttech-modern, hunttech-modern-dark, hunttech-modern-light): правило было рассчитано на старую компоновку из 4 слотов (ID/название/грейд/кнопка) — слот 1 (теперь поле «Вакансия») получал `flex: 0 1 100px` и не растягивался, а слот 2 (теперь кнопка «Генерировать») — `flex: 5 1 160px` и занимал всё свободное место; теперь слот 1 — `flex: 5 1 160px` (поле тянется), слот 2 — `flex: 0 0 auto` (кнопка по содержимому); мертвые правила слотов 3–4 удалены; `buildScssThemes` PASS |
| 2026-08-06 | «Город + Добавить» вынесены из строки «Компания + Департамент» в отдельную строку 4 `vacancyCityRow` (под строкой 3 `vacancyCompanyRow`); у `cityFieldsHBox` убран внешний ratio 1 (в своей строке работает `expand` на город); business-id, bindings, options, actions, required не менялись; Java не затрагивался |
| 2026-08-06 | Компоновка карточки «Вакансия»: удалёнка `remoteWorkField` перенесена в строку 1 (`vacancyTopRow`) перед датой закрытия (`width=AUTO`); строки «Компания + Департамент» и «Город + Добавить» объединены в одну строку 3 `vacancyCompanyCityRow` (равные доли) под строкой «Должность/Проект»; комментарий к удалёнке — строка 4 `remoteCommentRow`; удалены обёртки `vacancyParamsCompanyRow`/`vacancyParamsCityRow`; business-id, bindings, options, actions, required не менялись; Java не затрагивался |
| 2026-08-06 | Строка `vacancyNameHBox`: устранён конфликт `expand="vacansyNameField"` (hbox) с `box.expandRatio="5"` у поля — у поля убран expandRatio (edit и preview, там был ratio 6), кнопка «Генерировать» остаётся `width=AUTO` без ratio; теперь растягивается строка ввода «Вакансия», кнопка — по содержимому |
| 2026-08-06 | Возвращена область ввода «Удалёнка» `remoteWorkField` в строку 2: `width="25%"` (без expandRatio — поле схлопывалось в строке с expand-соседями) заменён на рабочий `box.expandRatio="2"` + `width="100%"` (пропорция строки 3:4:2); комментарий к удалёнке остался на отдельной строке 3 `remoteCommentRow` |
| 2026-08-06 | Скрыты чекбоксы «Черновик вакансии» (`signDraftCheckBox`) и «Эксклюзивная вакансия» (`internalProjectCheckBox`): `visible="false"` в `open-position-edit.xml` и `open-position-edit-preview.xml`; в `OpenPositionEdit.java` `setInternalProject()` перестал вызывать `setVisible` (иначе менеджеры/админы снова показали бы чекбокс) — признак по-прежнему выставляется логикой проекта; компиляция PASS; строка `vacancyFlagsRow` сохранена (все три чекбокса в форме, `openClosePositionCheckBox` служебный) |
| 2026-08-06 | Кнопка «Генерировать» `generateVacancyNameFieldButton`: явный `width="AUTO"` в `open-position-edit-preview.xml` (в основном дескрипторе уже был `width="AUTO"`); align `BOTTOM_RIGHT` и `invoke` сохранены |
| 2026-08-06 | Строка 2 `vacancyMainRow`: удалёнка `remoteWorkField` → `width="25%"` (убран expandRatio) — ровно 25% ширины строки; комментарий к удалёнке `remoteWorkCommentField` вынесен в отдельную строку 3 `remoteCommentRow` (на всю ширину); business-id, bindings, options, actions и required не менялись; Java не затрагивался |
| 2026-08-06 | Компоновка карточки «Вакансия»: строка 1 `vacancyTopRow` — ID + Грейд + Приоритет + Дата закрытия (все по содержимому); новая строка 2 `vacancyMainRow` — Должность (ratio 3) + Проект (ratio 4) + Удалёнка (ratio 2) + Комментарий к удалёнке (ratio 3); удалены обёртки `closingDateSignFieldsHBox` и `remoteWorkFieldsHBox`; названия `vacansyNameField` + «Генерировать», чекбоксы, фильтры, комментарий приоритета, компания/департамент и город — ниже без изменений; business-id, bindings, options, actions и required не менялись; Java не затрагивался |
| 2026-08-06 | Ширины строки 1 карточки «Вакансия» (`vacancyTopRow`): ID `vacansyIDTextField` → `width=AUTO` (убран `expandRatio` и 100%), грейд остался `width=AUTO`; должность `positionTypeField` ratio 2→3, проект `projectNameField` ratio 3→4 — освободившееся место отдано должности и проекту (пропорция 3:4); синхронизирован `open-position-edit-preview.xml` (ID → AUTO); business-id и bindings не менялись |
| 2026-08-06 | Перекомпоновка карточки «Вакансия» (`openPositionEditorIdentifiersCard`): строка 1 `vacancyTopRow` (ID + Грейд + Должность + Проект в одну линию), строка 2 `vacancyNameHBox` (название вакансии + «Генерировать»), строка 3 `closingDateSignFieldsHBox` (Приоритет + Удалёнка + Дата закрытия); признаки (`vacancyFlagsRow`), фильтры проектов, комментарии, компания/департамент, город перенесены ниже без потери полей; удалены обёртки `closingDateFieldsHBox`, `vacancyParamsPositionRow`, `vacancyParamsProjectRow`, `projectFieldsVBox`; business-id, bindings, options, actions и required не изменялись; Java-контроллер не затрагивался |
| 2026-08-06 | Вложенная секция «Количество персонала» (`personnelCountGroupBox`: `numberPositionHBox` → `numberPositionField` + `more10NumberPositionField`) перенесена из карточки «Вакансия» (`openPositionEditorIdentifiersCard`) в блок «Команда / Вакансия» (`commandOrVacancyGroupBox`, в конец после `commandOrVacancyHBox`); business-id, bindings и required не изменялись; sidebar-комментарий актуализирован; деплой не выполнялся |
| 2026-08-06 | В карточку «Вакансия» (`openPositionEditorIdentifiersCard`) перенесены все элементы блока «Параметры вакансии» (`projectTypeGroupBox`): обёртка удалена (caption `msgVacancyParams` больше не выводится), строки `vacancyParamsPositionRow` → `remoteWorkFieldsHBox` → `vacancyParamsProjectRow` → `vacancyParamsCompanyRow` → `vacancyParamsCityRow` → `personnelCountGroupBox` вставлены после `priorityFieldsHBox` в прежнем порядке; business-id, bindings, options, actions и required не изменялись; sidebar-комментарии актуализированы; label-навигация (NavProject → фокус `positionTypeField`) не затрагивалась |
| 2026-08-06 | Компоновка карточки «Вакансия» (`openPositionEditorIdentifiersCard`): все элементы блоков «Вакансия» (`vacancyInputGroupBox`) и «Настройки вакансии» (`commandFieldHBox`) вынесены из groupBox-обёрток напрямую в карточку (обёртки удалены; их captions «Вакансия»/«Настройки вакансии» упразднены); строки `vacancyNameHBox` → `vacancyTitleSpacerHBox` → `closingDateFieldsHBox`/`closingDateSignFieldsHBox` → `priorityFieldsHBox` сохранены в прежнем порядке; business-id, bindings, actions, required и ширина грейда `width=AUTO` не изменялись; sidebar-комментарий актуализирован; preview-XML не затрагивался (preview-контроллер держит `commandFieldHBox`/`vacancyTitleSpacerHBox` через @Named) |
| 2026-08-06 | Компоновка блока «Вакансия» (`vacancyInputGroupBox`): ID/Вакансия/Грейд/«Генерировать» подтверждены в строке `vacancyNameHBox` (перемещения не потребовались); ширина `gradeLookupPickerField` переведена на `width="AUTO"` (убран `box.expandRatio="2"`) — грейд занимает ширину по содержимому, свободное место делится между ID (ratio 1) и названием (ratio 5); preview-XML синхронизирован; бизнес-логика и bindings не изменялись |
| 2026-08-06 | Вкладка «Проект» (`tabOpenPosition`) переименована в «Вакансия»: caption вкладки переключён на `msg://msgVacansy` (ключ `msgNamePosition` сохранён для подписи поля «Проект позиции»); карточка «Идентификаторы и статус» (`openPositionEditorIdentifiersCard`) переименована в «Вакансия» (значение mainMsg-ключа `openPositionEditorIdentifiersCard`); актуализированы inline-комментарии XML (edit + preview) |
| 2026-08-06 | Подписи всех элементов OpenPositionEdit приведены к стилю IteractionListEdit (эталон контракта Edit-экранов, `HRM_HuntTech_Edit_Screen_Shared_Style_Contract.md`; параметры взяты из общего SCSS/CSS текущего стиля): (1) подписи полей `.open-position-editor-field-row .v-caption` — `mix($v-font-color,$v-panel-background-color,72%)` (было 74%), `font-size 13px` (было 12px), `line-height 17.55px` (было 17px; = 13px × 1.35, как `.v-captiontext` IteractionListEdit); (2) заголовки секций `.edit-accordion-section .v-panel-caption` — `mix 90%` (было 92%), `line-height 23px` (было 22px) — как `.iteraction-list-flat-section-title`; (3) sidebar-подписи `.open-position-editor-summary-caption` (инфокарточка «Информация» + блок срочности) — `rgba(248,250,252,0.62)` (было 0.58), `12px` (было 10px), `line-height 18px` (было 15px), + `letter-spacing 0.04em` — как `.iteraction-list-sidebar-caption`. Браузерная сверка (CDP): полевые 13px/600/rgb(108,118,128)/17.55px, секции 16px/700/rgb(72,84,96)/23px, sidebar 12px/600/rgba(248,250,252,0.62)/18px/0.48px — идентично IteractionListEdit. Чекбоксы не изменены (утверждённый стиль 12px/600, крупная галочка 20px) |
| 2026-08-06 | Чекбоксы OpenPositionEdit: стилизованная галочка увеличена на 50% — квадратик `label::before/::after` 13px→20px (width/height 20px, `::after` font-size 15px, line-height 20px, text-align center), отступ от подписи увеличен (label padding-left 17px→28px = 20px квадратик + 8px зазор). Шрифт подписи зафиксирован 12px в обоих состояниях: legacy-логика (`OpenPositionEdit.onSignDraftCheckBoxValueChange` :3489) ставит чекбоксу «Черновик вакансии» стиль `h2-gray` (font-size 18px bold gray) при включённом черновике — перекрыто `label { font-size: 12px !important }`. Браузерная проверка (CDP): квадратик 20×20, центр квадратика ↔ центр текста delta 1.2px, отступ ~8px, шрифт 12px в checked/unchecked, синяя галочка #4d7ab2 видна по центру квадратика, input скрыт (clip), toggle работает |
| 2026-08-06 | Чекбоксы OpenPositionEdit переработаны: виден один стилизованный квадратик темы (`label::before` — рамка `#9bb3d3` на белом, `label::after` — галочка ThemeIcons, при checked `#4d7ab2`), отцентрованный по вертикали текста подписи — `label { position:relative; padding-left:17px }`, `::before/::after { absolute; left:0; top:50%; transform:translateY(-50%) }`; нативный `<input>` скрыт и убран из потока (`absolute + clip: rect(0,0,0,0)`). Ранее: `opacity:0` при `position:static` оставлял подпись сдвинутой и квадратик у верха строки; `position:static` без скрытия давал дубль квадратиков. Браузерная проверка: input clip, квадратик виден, delta центр квадратика ↔ центр текста = 1.2px, toggle + синяя галочка работают |
| 2026-08-06 | Багфикс NPE при снятии чекбокса «Черновик вакансии»: `onSignDraftCheckBoxValueChange` снимает черновик через `priorityField.setValue(null)`, что триггерило `onPriorityFieldValueChange` с `event.getValue() == null` → NPE на `event.getValue().equals(OpenPositionPriority.LOW.getId())` (OpenPositionEdit.java:2181). Добавлена null-проверка перед equals (поведение при валидных значениях не изменено; View Integrity: новые атрибуты не запрашиваются) |
| 2026-08-06 | Устранён дубль квадратика с галочкой в чекбоксах OpenPositionEdit: после правки `8b6719f6` нативный `<input>` остался видимым (`position: static`), и рядом с ним тема рисовала стилизованный квадратик (`label::before`/`::after` на `span.v-checkbox` position:relative + padding-left 17px) — два квадратика. Добавлено `.open-position-editor .v-checkbox input { opacity: 0 !important }` (все 7 тем): виден только стилизованный квадратик темы, hit-область 13×13 и переключение по label сохранены; браузерная проверка: input opacity 0, toggle false→true работает |
| 2026-08-06 | Фон инфокарточки «Информация» в sidebar (`openPositionEditorInfoCard`) сделан точно как фон sidebar: `background: transparent` у карточки и её `v-panel-captionwrap`/`v-panel-caption`/`v-panel-content` (селекторы `.edit-sidebar .open-position-editor-info-card…` в `open-position-editor.scss` всех 7 тем) — сквозь карточку виден тот же градиент `.edit-sidebar` (#172638→#132130→#0f1b28); рамка/радиус/тень `edit-accordion-section` сохранены. Браузерная проверка: computed sidebar `#172638`, карточка `rgba(0,0,0,0)` (transparent), все 6 полей на месте и заполнены (Должность/Проект/Владелец/Город/МАХ ТК/МАХ ИП); канонический прогон 16/16 |
| 2026-08-06 | Подписи radio-кнопок формы OpenPositionEdit приведены к типографике подписей чекбоксов: `.open-position-editor .v-radiobutton label` — font-weight 600 (было 400), line-height 17px (было 12.5px), размер 12px совпадал; браузерная проверка: «Команда»/«Вакансия» (commandOrPosition) = 12px w600 lh17px, идентично чекбоксам |
| 2026-08-06 | Шрифт ввода всех текстовых полей формы OpenPositionEdit приведён к шрифту picker-поля «Грейд» (15px Verdana w400): `.open-position-editor .v-textfield`, `.v-textarea textarea`, `.v-datefield input` — 15px !important; поля владельца проекта (`.open-position-editor-owner-row .v-textfield` / `.borderless`) переведены с 12px на 15px прямо в их правиле (иначе специфичность с `.halo`-префиксом перекрывала); браузерная проверка: textfield/filterselect/datefield/owner-row — все 15px |
| 2026-08-06 | Чекбоксы формы OpenPositionEdit выровнены по вертикали и приведены к типографике подписей полей: подписи всех `.v-checkbox` получили шрифт как у caption Грейда (12px, w600, line-height 17px); галочка выровнена по центру подписи — `.open-position-editor .v-checkbox` переведён в inline-flex с align-items:center, у input сброшен absolute-positioning темы (`position: static !important`, margin 0 4px 0 0); проверено в браузере: delta=0 у всех чекбоксов (включая фильтры проектов и salary) |
| 2026-08-06 | Устранены наезды блоков OpenPositionEdit: Vaadin выносил шапку GroupBox над рамкой (inline `margin-top:-44px` у `v-panel-captionwrap`), из-за чего caption каждой карточки перекрывал низ предыдущего блока (16–30px: «Идентификаторы и статус» ↔ «Команда / Вакансия», «Команда / Вакансия» ↔ «Параметры вакансии», «Параметры вакансии» ↔ «Зарплатное предложение»); добавлено правило `.open-position-editor .edit-accordion-section > .v-panel-captionwrap { margin-top: 0 !important }` (шапка внутри панели, зазоры gap 14px работают честно); резерв group-tab («Согласование») сокращён с 58px до 14px (пустота устранена); браузерная проверка: наездов 0 на вкладке «О вакансии», вкладка «Согласование» чистая |
| 2026-08-06 | Заголовки блоков OpenPositionEdit приведены к единому стилю «шапки блока»: все `edit-accordion-section .v-panel-caption` — 16px w700 с акцентной плашкой слева (border-left 3px `$v-selection-color`); вложенные `open-position-editor-subsection` («Вакансия», «Настройки вакансии», «Количество персонала») — 13px w600 с полупрозрачной плашкой; плашка у `open-position-editor-primary-section` перенесена с панели на caption; добавлен отсутствовавший заголовок блока «Трудовой договор» (`laborAgreementGroupBox` caption `msgLaborAgreement`, новый msg-ключ) |
| 2026-08-06 | Первый блок «Вакансия» (`vacancyInputGroupBox`) перестроен в 3 строки: строка 1 `vacancyNameHBox` — ID (`vacansyIDTextField`, ratio 1) + Вакансия (`vacansyNameField`, ratio 5) + Грейд (`gradeLookupPickerField`, ratio 2) + кнопка `generateVacancyNameFieldButton` «Генерировать» (перенесены из удалённой строки `gradeActionRowHBox`); строка 2 `closingDateSignFieldsHBox` — Дата закрытия + Приоритет + `signDraftCheckBox` «Черновик вакансии» + `internalProjectCheckBox` «Эксклюзивная вакансия» (стал видимым, убран visible=false; Java-управление по ролям сохранено) + скрытый `openClosePositionCheckBox`; строка 3 `priorityFieldsHBox` — комментарий `commentPriority` с caption «Комментарий» (`msgOpenPositionComment`) на всю ширину; удалены `gradeActionRowHBox`, `vacancyTitleSpacerHBox`, `closingDateCheckBoxesHBox` (не контрактные); контрактные groupBox-секции (`commandFieldHBox` и др.) сохранены; preview-XML синхронизирован |
| 2026-08-06 | В sidebar OpenPositionEdit под наименованием вакансии добавлен блок срочности `vacancyPrioritySummary` (копия IteractionListEdit, stylename `open-position-editor-priority-summary`): первый элемент — приоритет со светофором (label `msgPriority` «Приоритет:», hbox `vacancyPriorityValueBox` с image `trafficLighterImage` и label `currentPriorityLabel`, значения и иконки `StandartPriorityVacancy` — Draft/Paused/Low/Normal/High/Critical); второй элемент — формат работы: label `msgRemoteWorkSidebar` «Работа:» + `remoteWorkSidebarLabel` (Офис / Удаленная работа / Гибрид (50/50) по `remoteWork` 0/1/2); значения формирует `refreshSidebarPriority()` (presentation-only, вызывается из `refreshSidebarInfoCard()` — onBeforeShow + listener openPositionDc); новые msg-ключи `msgPriority`, `msgRemoteWorkSidebar`; иконки traffic-lights_*/remove.png скопированы в темы hunttech-modern* (отсутствовали); preview-XML дополнен скрытыми компонентами (контракт @Inject) |
| 2026-08-06 | В sidebar OpenPositionEdit после label-навигации добавлена информационная карточка в рамке `openPositionEditorInfoCard` (caption `msgSidebarInfoCard` «Информация», `edit-accordion-section` + `open-position-editor-info-card`): вертикальный список HTML-label `infoPositionLabel` («Должность» → `vacansyName`), `infoProjectLabel` («Проект» → `projectName.projectName`), `infoOwnerLabel` («Владелец проекта» → `projectName.projectOwner` firstName+secondName), `infoCityLabel` («Город» → `cityPosition.cityRuName`), `infoSalaryMaxTcLabel` («Зарплата МАХ (ТК)» → `salaryMax`) и `infoSalaryMaxIeLabel` («Зарплата МАХ (ИП)» → `salaryIE`); значения формирует `refreshSidebarInfoCard()` (presentation-only, HTML с экранированием) — вызывается в onBeforeShow и через `openPositionDc.addItemPropertyChangeListener` при изменении любого атрибута; данные доступны в view (projectName → project-edit-view → projectOwner → person-owner-view), View Integrity пройдена; новые msg-ключи `msgSidebarInfoCard` |
| 2026-08-06 | Секция «Заработная плата» перестроена в блок **«Зарплатное предложение»** (`salaryGroupBox`, caption `msgSalaryProposal`): строка 1 `salaryMinMaxHBox` — мин/мах зарплата в линию, строка 2 `salaryIEHBox` — зарплата ИП, строка 3 `salaryCheckBoxesRow` — чекбоксы «Ориентируемся на запрос кандидата» + «Фиксированный лимит зарплаты», строка 4 `salaryCommentRow` — комментарий на всю ширину; отдельная label-навигация: пункт `openPositionEditorNavSalary` переименован в «Зарплатное предложение» (caption `msgSalaryProposal`), клик → подсветка + фокус в `openPositionFieldSalaryMin` (`onOpenPositionEditorNavSalaryClick`); бизнес-id, bindings и свойства полей сохранены; preview-XML не изменялся |
| 2026-08-06 | Поля «ID» (`vacansyIDTextField`) и «Вакансия» (`vacansyNameField`) объединены в единый блок ввода `vacancyInputGroupBox` (subsection `open-position-editor-vacancy-section`, caption «Вакансия») с расположением в одну линию (строка `vacancyNameHBox`, ID ~130px + название на расширение); добавлена отдельная label-навигация: пункт `openPositionEditorNavVacancy` (mainMsg-ключ `openPositionEditorNavVacancy=Вакансия`) — клик снимает подсветку со всех пунктов, подсвечивает «Вакансия» и фокусирует `vacansyIDTextField` (штатная прокрутка ScrollBox; рефакторинг: общий `resetNavigationActiveStyles()` вместо дублирующих removeStyleName); в preview-XML добавлена скрытая кнопка для контракта @Named; business-id, bindings и required не изменялись |
| 2026-08-06 | Секция «Проект, Компания, Тип должности» перестроена в единый блок **«Параметры вакансии»** (`projectTypeGroupBox`, caption `msgVacancyParams`): Должность на всю ширину (`vacancyParamsPositionRow` → `positionTypeField`), «Удалёнка» + «Комментарий» в линию (`remoteWorkFieldsHBox`), «Проект» на всю ширину (`vacancyParamsProjectRow` → `projectFieldsVBox` + фильтры), «Компания» + «Департамент» в линию (`vacancyParamsCompanyRow`), «Город» + «Добавить» в линию (`vacancyParamsCityRow` → `cityFieldsHBox`), «Количество персонала» — вложенная секция `personnelCountGroupBox` (перенесена из cardsRow2, добавлен `open-position-editor-subsection`; cardsRow2 теперь только «Заработная плата»); отдельная label-навигация: пункт `openPositionEditorNavProject` переименован в «Параметры вакансии» (значение ключа `openPositionEditorNavProject`), переставлен после «Настройки вакансии», клик → подсветка + фокус в `positionTypeField` (`onOpenPositionEditorNavProjectClick`); новые msg-ключи `msgVacancyParams`; business-id, bindings, required, invoke и фильтры проектов сохранены; preview-XML не изменялся |
| 2026-08-05 | Исправлено смещение текста пунктов label-навигации sidebar OpenPositionEdit: текст выпадал ниже подсветки (из-за `display: block` у Vaadin-кнопки); кнопки `.v-button-label-nav-item` стали flex-контейнерами (`display: flex; align-items: center`, wrap `flex: 1`) в shared `edit-screen-shared-styles.scss` (7 тем синхронно) — текст центрируется внутри подсветки; проверено на 1440/1366 в halo и hunttech-modern-dark, сценарий переключения вкладок работает |
| 2026-08-05 | Исправлен наезд пиктограмм опций на текст в picker-полях OpenPositionEdit («Приоритет вакансии», «Удалёнка», «Регистрация для работы»): локальное правило `.edit-form-control.v-filterselect > .v-icon` (иконка 20px по центру слева) + `padding-left: 40px` у input (shared-селектор не матчился, т.к. stylename стоит на самом `.v-filterselect`); полю `priorityField` добавлен локальный класс `open-position-editor-priority-field` (`flex: 0 0 150px`) — текст опции читается на 1920/1440/1366/1280, light/dark |
| 2026-08-05 | Скорректирована визуальная компоновка вкладки «Проект» OpenPositionEdit: синхронизирована label-навигация (вердикт арбитра `00-arbitration-sidebar-active.md` — 6 `@Named` + `label-nav-item-active` в `onTabSheetOpenPositionSelectedTabChange`), устранено переполнение Tab, восстановлены горизонтальные строки ID/Вакансия и Грейд/«Генерировать» (`gradeActionRowHBox`), блоки ID/«Команда разработчиков» и «Количество персонала»/«Заработная плата» размещены вертикально (`open-position-editor-cards-row` → column); бизнес-логика не изменялась. |
| 2026-08-05 | Выполнен визуальный редизайн формы по UI-контракту: двухпанельная компоновка `edit-screen-layout` + sidebar/workspace, label-навигация (эталон IteractionListEdit, пункты без invoke), карточки `edit-accordion-section`, responsive-строки полей, варианты таблиц/редакторов variant5, footer с primary/secondary actions; диалог увеличен до 1400×900 (арбитр 9-1); платёжные секции перенесены во вкладку «Трудовой договор» (арбитр 9-2), `tabPayments` остаётся скрытой; добавлены 7 идентичных partial `open-position-editor.scss` и их подключение в 7 темах; добавлены визуальные msg-ключи и контрактный тест `OpenPositionEditLayoutContractTest`. Бизнес-логика, сущности, справочники и другие формы не изменялись. |
| 2026-08-01 | Создание Spec; полное inline-документирование XML (168 комментариев), бизнес-id всем элементам (buttonsPanel→laborAgreement/someFiles/openPositionNewsButtonsPanel, id для всех hbox/vbox/groupBox-контейнеров и кнопок), javadoc-покрытие контроллера (163 метода + класс). |
