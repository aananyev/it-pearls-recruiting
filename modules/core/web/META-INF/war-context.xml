<Context>

    <!-- Database connection -->
    <Resource
      name="jdbc/CubaDS"
      type="javax.sql.DataSource"
      maxTotal="20"
      maxIdle="2"
      maxWaitMillis="5000"
      driverClassName="org.postgresql.Driver"
      url="${env.HUNTTECH_DB_JDBC_URL}"
      username="${env.HUNTTECH_DB_USER}"
      password="${env.HUNTTECH_DB_PASSWORD}"/>

    <!-- Switch off session serialization -->
    <Manager pathname=""/>

</Context>
